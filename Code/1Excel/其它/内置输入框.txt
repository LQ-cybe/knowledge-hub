    Dim fileName As String
    fileName = InputBox("请输入要保存的文件名（不含扩展名）", "文件名输入")

    ' 检查用户是否输入了文件名
    If fileName = "" Then
        MsgBox "未输入文件名，操作已取消！"
        Application.DisplayAlerts = True
        Application.ScreenUpdating = True
        Exit Sub
    End If