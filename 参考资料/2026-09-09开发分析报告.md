# 2026-09-09 开发分析报告

> 项目：knowledge-hub（本地 Web 架构知识库软件）
> 时段：今日共 2 个功能提交（`2dfdb26` 第 15 轮、`3473c65` 第 16 轮），均已推送 GitHub（main）
> 关联文档：《更新日志.md》第 15/16 轮条目、《功能说明文档.md》第二十四/二十五章
> 本轮主题：历史视图 + 笔记/待办独立页（第 15 轮）→ 图谱缩放体系重做 + 主目录语义 + 编辑器返回来源页（第 16 轮）

---

## 一、今日总览

今天完成两条主线，均围绕"图谱可视化体验 + 资源管理语义"展开：

1. **第 15 轮（历史视图 + 笔记/待办独立页 + 图谱 6 项修复）**：时间线改名「历史」并增强"已修改"语义；新增笔记、待办两个独立 Tab 页面（含后端 `done` 字段持久化）；图谱 6 项修复——关系图标签切换稳定、桑基图 SVG 矢量缩放、矩形树图 drill-down 下钻重构、旭日图实心中心、打包图不重叠、统一缩放策略。
2. **第 16 轮（图谱缩放体系重做 + 浏览主目录语义修正 + 编辑器返回来源页）**：用户一次性提 8 点——旭日图缩放方向修复（双向）、关系图标签防跳动、树图 wheel 补偿、矩形树图根块"返回上一级"、去描边改颜色对比、旭日图径向标签、浏览主目录不再混排、编辑器返回来源页。本轮含 **2 个深度根因排障**（见下）与 **1 个自引入 Bug 的当场修复**。

---

## 二、关键点分析

### 1. 第 15 轮：三个新页面 + 图谱体验修复（提交 `2dfdb26`）

| 模块 | 核心交付 | 技术要点 |
|---|---|---|
| 历史视图 | 时间线改名「历史」，`updated_at ≠ created_at` 显示蓝色「已修改」标记 | 后端 `/api/timeline` 增加 updated_at 返回列 |
| 笔记页 | 独立 Tab：列表/搜索/分页/右键菜单/新建后自动进编辑器 | NotesView.vue；`mode='note'` 全屏 MD 编辑器 |
| 待办页 | 独立 Tab：完成勾选/筛选/右键标记完成/新建 | TodosView.vue；后端 `PUT /resources/:id` 新增 **done 字段**持久化 |
| 图谱 6 项 | 关系图标签切换布局稳定；桑基 SVG 矢量缩放；矩形树图 drill-down 下钻；旭日图实心中心；打包图不重叠；缩放策略统一 | `ensureRenderer()` 按 canvas/svg 重建实例；`colorize(startDepth)` 分色同源；drillStack 下钻栈 |

### 2. 第 16 轮：图谱缩放体系统一（提交 `3473c65`）

| 诉求 | 方案 | 关键实现 |
|---|---|---|
| ① 标签缩放方向反 | 旭日图 `sunZoom` 改 **0.6..1.8 双向**，radius 正比、字号 `sunLabelSize()` 双向同步 | 放大文字变大、缩小文字变小（实测 1.00→1.08→1.17x） |
| ② 关系图标签跳动 + 桑基平移/光标中心缩放 | 关系图只 patch label（见排障二）；桑基图指针拖拽平移 + `t'=t+p*sOld*(1-factor)` 光标中心缩放 | applySankeyTransform 容器 transform；bindSankeyPan |
| ③ 树图放大节点不缩小 | wheel 补偿：`symbolSize = min(10,max(2,6.5/z))` 反向缩小、字号 `min(22,max(8,7/z+3))` 放大 | 树图 roam + 补偿共存 |
| ④ 矩形树图 undefined/无返回语义 | tooltip 兜底 + formatter 闭包注入"← 返回上一级"（见排障一） | 根块中性色 #3a4356 + 浅色文字 |
| ⑤ 描边辨识度差 | 全系列移除 textBorder，改深浅主题对比色（暗 #d1d5db / 浅 #374151） | 关系图实测文字清晰 |
| ⑥ 旭日标签重叠 | levels `rotate='radial'` 径向放射 | 替代 tangential |
| ⑦ 主目录混排 | `parentId` 恒传根文件夹 id，废弃 `parentId='root'` 语义 | 浏览页实测 = Code 内部 16 项 |
| ⑧ 编辑器返回错误页 | App.vue `editorFrom` 记录来源 Tab，关闭返回并 reload | NotesView/TodosView 暴露 reload；行光标 pointer |

### 3. 排障攻坚一：矩形树图 "undefined·文件 1495 个" + 根块无返回语义（第 16 轮最长单点）

**现象**：矩形树图最外侧块 tooltip 显示 `undefined·文件 1495 个`；点击组下钻后根块没有"返回上一级"提示。

**三重根因**：
1. **ECharts treemap 对 `data[0]` 根块加工后 p.name 丢失（undefined）**——tooltip formatter `p.name` 取到 undefined → 显示"undefined"；series label formatter 兜底 `p.name || '所有文件'` 掩盖了根块真实名称。
2. **data 项级 label 覆盖对 treemap 根块不生效**——初版给 `tmData[0]` 写 label formatter 无效，必须经 series label/upperLabel 的 formatter 闭包注入（捕获渲染时的 drillStack 状态）。
3. **tooltip 无兜底**——`n.name || n.title || '所有文件'` 统一兜底，不再出现 undefined。

**验证**：浏览器实测——未下钻根块「所有文件」、下钻 1Excel 后「← 返回上一级」+ 块名「1Excel」、tooltip「所有文件·文件 1495 个 48.79 MB」、点击根块返回上一级，全部通过。

### 4. 排障攻坚二：关系图"关闭标签后整图空白"（第 16 轮自引入 Bug，当场发现并修复）

**现象**：开启标签正常；**关闭标签后整图变纯黑空白**。

**根因链**：本轮初版为防跳动采用"读回 force 收敛坐标再写回"方案——`chart.getOption()` 读回的 graph `series[0].data` 项**不含布局坐标 x/y**（布局坐标在 ECharts 内部，不写回用户数据）→ 坐标收集循环全落空 → `pos` 为空数组 → setOption 合并 `data: pos.length ? pos : undefined` = **`data: undefined` → 清空整图**。

**修复**：关系图标签切换**只 patch `series[0].label.show`**——data 引用不变 → force 布局不重启 → 天然不跳动、不空白。浏览器实测开→关→开反复切换布局稳定。

**教训**：试图"读回内部状态再写回"的防跳动方案比"只改需要的字段"风险高得多；ECharts 的 getOption 只保证返回用户提交的数据结构，不保证含引擎内部布局产物。

### 5. 语义修正（第 16 轮）

- **浏览主目录**：第 14 轮为"在主目录看到自建资源"引入 `parentId='root'` 语义，导致主目录混排 Code 自身 + 全部顶层笔记/书签/待办——本轮废弃该语义，parentId 恒传根文件夹 id（指向 Code 内部），自建资源回归各自独立页管理。
- **编辑器返回来源页**：新增 `editorFrom` 记录来源 Tab（browse/notes/todos），关闭编辑器切回来源页并 reload——修复"从笔记页打开、关闭却回到浏览页"。

### 6. 数据治理

物理删除测试数据：待办 `r15-test-todo`（142ca5b0…）、笔记「测试」（88a1c13e…）；保留「浏览器验证笔记」供后续验证。库内当前 1643 资源（书签 1 / 文件 1495 / 文件夹 141 / 笔记 1 / 报表 2 / 待办 3）。

---

## 三、耗时点统计（估算）

| 阶段 | 约耗时 | 说明 |
|---|---|---|
| 第 15 轮：历史 + 笔记/待办页 + 图谱 6 项 | ~2.5h | 3 个新页面 + 后端 done 字段 + 图谱 6 项，含浏览器实测 |
| 第 16 轮：8 项诉求 + 2 深度排障 | ~2.5h | 其中矩形树图根块排障（含浏览器多轮点击验证）与关系图空白 Bug 各占较大比重 |

### 耗时结论
- **矩形树图根块排障是今日单点之最**：初版"data 项 label 覆盖"方案无效后，需绕道 formatter 闭包注入——ECharts treemap 根节点的 data 加工行为（p.name 丢失、data 级覆盖不生效）需结合浏览器实测才定位。
- **关系图空白 Bug 证明"自引入回归"的风险**：防跳动方案本身引入了更严重的清空 Bug，最终简化方案（只 patch label）同时解决跳动与空白。
- **canvas 类交互验证的代价**：点击 treemap 触发 1495 块重排时主线程阻塞，bu 截图多次超时（需等待重排完成或先 reload 恢复会话）——重活视图验证节奏需放慢。

---

## 四、经验与结论

1. **ECharts 高级定制必须实测数据层行为**：treemap 根块 p.name 丢失、data 级 label 覆盖不生效、getOption 不含布局坐标——三个都只能靠浏览器实测暴露，文档不会告诉你。
2. **防跳动类方案优先"最小 patch"**：只改目标字段（label.show），不读回、不重写 data，从源头避免 force 重启与数据清空两类风险。
3. **语义回归要随需求演进清理**：第 14 轮"root 语义"在笔记/待办独立页落地后即成历史包袱，本轮及时废弃——临时语义应在其使命结束时主动移除，而非长期共存。
4. **每轮"先验证、后文档、再提交"铁律**维持：本轮 8 项全部浏览器实测通过后才更新文档并推送。

---

## 五、下一步建议

- 继续深化书签/笔记功能（用户已列需求：书签导入、采集文章、时间线按类型筛选等）。
- 思维导图剩余项（如导出 PDF/图片、大纲与布局面板宽度记忆）。
- 「浏览器验证笔记」遗留数据可随时物理删除。
