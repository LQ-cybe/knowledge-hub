# Knowledge Hub 本地知识库 — 功能说明文档

> 版本：v0.6（2026-09-07）
> 项目位置：`D:\编程开发\开源项目\knowledge-hub`
> 技术栈：本地 Web 架构（Vue3 + Element Plus / Node.js + TypeScript / SQLite + FTS5），纯本地运行，无云端依赖。

---

## 一、整体架构

软件分为**两层 + 一个入口**：

```
┌─────────────────────────────────────────────┐
│  渲染层（前端 web/，Vite :5178）            │
│  工作台 · 浏览 · 图谱 · 时间线 · 报表        │
│  Element Plus 组件 + ECharts 图表            │
└──────────────────┬──────────────────────────┘
                   │ HTTP /api（axios 代理）
┌──────────────────▼──────────────────────────┐
│  业务层（后端 server/，Node :5177）          │
│  路由 routes/：resources · reports           │
│  规则：扫描、打标、搜索、报表周期计算        │
└──────────────────┬──────────────────────────┘
                   │ better-sqlite3
┌──────────────────▼──────────────────────────┐
│  数据层（SQLite knowledge.db）              │
│  resources / tags / resource_tags /         │
│  note_links / reports / attachments         │
│  + FTS5 全文索引（三触发器同步）            │
└─────────────────────────────────────────────┘
```

- **数据层**：所有实体（文件、文件夹、笔记、书签、待办、标签、报表）统一入库，每个资源带唯一 `id`（UUID）；标签通过 `resource_tags` 关联表多对多映射，与磁盘文件内容零耦合。
- **业务层**：保存/修改规则 + 界面操作执行规则（如"打标可递归到子文件"、"报表周期计算"、"搜索排序策略"）。
- **渲染层**：只负责取数、布局、交互与图表展示，不持有业务状态。

> 通用结论：任何软件都可抽象为"数据层 + 业务规则 + 渲染层"三层；本项目的价值在于把文件系统镜像成结构化数据库，再提供多维展示与批量操作。

---

## 二、数据表结构（SQLite）

| 表 | 用途 | 关键字段 |
|---|---|---|
| `resources` | 统一资源表（文件/文件夹/笔记/书签/待办/报表） | `id, type, title, path, parent_id, content, size, done, created_at, updated_at, status` |
| `tags` | 标签定义 | `id, name, color` |
| `resource_tags` | 资源↔标签多对多 | `resource_id, tag_id` |
| `note_links` | 笔记双链（引用维度，M5 后用） | `source_id, target_id` |
| `reports` | 已保存报表 | `id, title, type, content_md, content_html, period_start, period_end, created_at` |
| `attachments` | 附件 | 预留 |
| `resources_fts` | FTS5 全文索引（虚拟表） | `title, content`，由 `resources_ai/ad/au` 三个触发器在插入/删除/更新时自动同步 |

**关键设计**：文件/文件夹是磁盘扫描的镜像（`scanner` 增量扫描），**标签是独立于磁盘的本地元数据**——标签状态丢失不影响文件内容，重扫/改名均不触碰标签（仅需在扫描时保留 id 映射）。

---

## 三、核心功能模块

### 1. 工作台
- 顶部统计：资源总数 / 文件数 / 文件夹数（类型分布）
- 常用标签卡片（按使用次数 TOP10，点击跳转浏览页按该标签过滤）
- 顶层目录卡片（文件数/子目录数/占用空间）
- 最近更新文件列表

### 2. 浏览（= 原"浏览 + 数据库"合并页）
- 左侧**文件结构树**：Code 主目录下逐级目录树；默认全部折叠；支持全部展开/全部折叠；行高 21px
- 右侧**表格 / 卡片**双视图切换：
  - 表格：类型 / 标题 / 路径 / 标签 / 大小 / 更新时间，行高 29px，标签统一**主色蓝底白字**
  - 卡片：缩略图（图片类文件预览）/ 图标 + 标题 + 元信息 + 标签
- 筛选：标题/路径关键字、类型、标签、更新时间排序；**全文搜索（FTS5）**
- **打标**：单文件/批量打标（可勾选"同时应用到所有子文件和子文件夹"递归）；弹窗内可直接新建标签
- 分页：共 N 条 / 每页 N 条 / 前往第 N 页（全中文）

### 3. 图谱
- **三维度**：文件夹 / 标签 / 引用（双链，M5 后可用）
- **四视图**：
  - **关系图**：力导向布局，可拖拽，动画已关闭
  - **桑基图**：层级流向
  - **树图**：纵向层级树
  - **思维导图**：横向布局（根在左），节点按文件夹/文件着色，**点击节点折叠/展开，双击节点弹出打标对话框（即编辑）**
- **层级控制（所有视图通用）**：全部展开 / 全部折叠 / 折叠到第 N 层
  - 层数语义：**第 1 层 = 全部折叠（根 + 一级节点）**；第 N 层 = 显示到第 N 级；上限 = 树层级数 - 1
  - 实现：树图/思维导图用数据剪枝（`pruneTree`）+ 全展开，规避 ECharts `initialTreeDepth` 的层数偏移 bug；多根森林包一层隐藏虚拟根 `__forest__`
- **主题**：图表颜色随系统深色/浅色自动切换（监听 `kh-theme-change` 事件重渲染）

### 4. 时间线（垂直时间线）
- 按创建时间倒序分组（每天一组），左侧贯穿时间线 + 圆点 + 箭头
- 每条显示：名称 / 标签 / 时间或大小
- **筛选**：类型下拉 + **标签下拉**（含使用次数，`Access（163）` 格式），可清空
- **折叠**：每天一组可单独折叠（持久化到 sessionStorage）；顶部「全部折叠 / 全部展开」按钮（带边框方格样式，紧跟筛选）
- 行高紧凑；筛选行固定在页面顶部

### 5. 报表中心
- 五类周期：日报 / 周报 / 月报 / 季报 / 年报（按资源 `created_at` 落入统计区间计算）
- 报表内容：周期内新增资源明细 + 热门标签排行 + 类型分布
- 可编辑（Markdown）、导出（.md / .html）、保存（写入 `resources` type='report' + `reports` 表）
- 右侧已保存报表列表

---

### 6. 思维导图（独立导图工作区，参考 NexaNote 织记）
- **引擎**：**simple-mind-map v0.14.0-fix.3**（国产 MIT 开源引擎，已 `npm install`；组织图/概要/边界/关联线/节点样式/多选拖拽全部内置）。此前自研布局（ECharts tree / 自由坐标算法）被用户多次否决，最终切换到成熟引擎。NexaNote-main 源码（Flutter+FastAPI 手写笔记应用）经核查无思维导图代码，不可移植。
- **导图库**：多张导图管理（新建/打开/删除，显示节点数与更新时间）
- **编辑器**（三栏布局，对齐 NexaNote 界面）：
  - 顶部工具栏：＋子主题 / ＋同级 / 概要 / 边界 / 关系 / 缩放（40%~200%）/ 已保存状态
  - 左侧布局面板：**自由编辑 / 向右 / 向左 / 组织图 / 放射**（快捷键与多选等操作提示已移入本文档「八、思维导图交互说明」，界面不再显示文字提示）
  - 右侧节点属性：**文档主题**（Nexa 明亮 / Nexa 深色 / 经典分支 / 音蓝架构）、节点颜色（6 色覆盖，写 `data.fillColor`）、节点形状（自动/矩形/圆角/椭圆，写 `data.shape`）
- **编辑能力**：单击选中（右侧面板联动）、双击改名、Enter 同级、Tab 子级、Delete 级联删除、自由布局拖拽、概要（`ADD_GENERALIZATION`，数据 `data.generalization`）、边界（自定义整体矩形，见下）、关联线（`ADD_ASSOCIATIVE_LINE`，数据为 `data.associativeLineTargets` **uid 字符串数组**）
- **多选才可用的三按钮**：工具栏「概要 / 边界 / 关系」必须**选中 ≥2 个节点**才可点击（单选时置灰禁用），通过 Shift 多选或按住选择实现；"选中 2 个节点"是三者共同的交互前提
- **概要（多行直接编辑）**：点「概要」按钮后**不弹窗**，直接在节点内进入可编辑文本框（contenteditable），可输入**多行文本**；回车换行、失焦/回车提交并自动保存
  - 多行实现要点：概要文本在 SMM 数据中必须是**多个 `<p>` 段**（`<p>行1</p><p>行2</p>`），纯文本 `\n` 会被 SMM `htmlEscape` 吞掉换行；DB 存储用 `\n` 分隔（`</p>`→`\n` 转换），载入时按 `\n` 拆成多 `<p>`
- **边界（整体矩形语义，已否决 SMM 原生 outerFrame）**：多选 ≥2 个节点后点「边界」，在**这些节点的外围创建一个整体虚线矩形**包裹（不是给每个节点单独挂框）；矩形可单击选中（红边高亮）、**双击文字改名**、Delete 删除；矩形与节点同层，位于节点之下不遮挡内容
- **关系线（底层可点选）**：多选 ≥2 个节点后点「关系」创建（选中顺序即方向，先选为起点）；线绘制在**节点底层**（`associativeLineIsAlwaysAboveNode: false`，不盖节点内容）；**单击线可选中**（高亮变蓝）、**双击线中文字可内联编辑**（出现白底输入框）、Delete 删除
  - 可点选实现要点：SMM 的点击热区是**透明描边 path**，SVG 默认 `pointer-events` 不命中透明描边，需 CSS 强制 `.smm-associative-line-container path { pointer-events: stroke }`，且线文字 `pointer-events: none` 穿透到热区
- **自动保存**：`data_change`/`view_data_change` → 600ms 防抖 → 全量保存（nodes/links/members 四请求并行；服务器端 members 先删本图组员+清理孤儿组员再插）
- **数据模型**：`mindmaps`（导图，layout 存 free/right/left/org/radial）+ `mindmap_nodes`（节点，kind=node/boundary/summary，含颜色/形状）+ `mindmap_links`（关系连线 source→target）+ `mindmap_members`（边界/概要组员 group_id→node_id）
- **关键映射细节**（SMM 字段权威，勿凭记忆）：
  - SMM Node 实例：`node.getData('text'|'uid'|'fillColor'|'shape')`；`node.nodeData.data` 为数据体；虚拟根 `__VR__` 判断用 uid
  - 概要：DB summary（kind=summary, parent=所属节点）→ 载入挂 `data.generalization=[{text:'<p>…</p><p>…</p>', richText:true}]`；保存读 `data.generalization`，多 `<p>` 转 `\n`
  - 边界：**自定义 khBounds 运行时层**（DB boundary + members 构建，不向 SMM data 挂 outerFrame）；渲染时在 `.smm-container` svg 内插 `g.kh-bound-layer`（首个子元素、节点之下），用 `node.getRect()` 取成员包围盒 + padding 14 画虚线矩形 + 左上角名称文字；无边界时**必须移除空 `<g>`**（空 g 会让 SMM Scrollbar `rbox()` 崩溃致整图空白）；单击矩形选中、双击文字改名、Delete 删除，smmToDb 时写回 nodes(boundary 行)+members
  - 关联线：`data.associativeLineTargets` 是 **uid 字符串数组**（非对象数组）；节点激活事件为 `node_active`（v0.14 无 `node_click`）；`INSERT_CHILD_NODE(openEdit, [node])` 首参是 openEdit 布尔；激活的线通过 `clickPath` 变蓝显示（无 class 标记）
  - 旧 DB 数据 title 为双重转义 HTML（`&lt;p&gt;111&lt;/p&gt;`），载入过 `plain()` 解码剥离标签
- **图谱页与导图页分离**：图谱页（关系图/桑基图/树图）**不再包含思维导图入口**；思维导图仅在顶部「导图」页使用

---

## 四、搜索原理（重点）

### 1. 索引层：SQLite FTS5
- 建表 `resources_fts(title, content)`，tokenizer 为 `unicode61`（英文按词切分、中文按**单字**切分）
- 三个触发器自动同步：
  - `resources_ai`（INSERT 后）：新资源写入索引
  - `resources_ad`（DELETE 后）：删除索引条目
  - `resources_au`（UPDATE 后）：先删旧再插新（**改名/改内容后搜索立即生效**）

### 2. 查询层：双路合并
```
路径 A（全文命中）：resources_fts MATCH '"关键词"*'
  └── JOIN resources 取元数据，snippet() 生成高亮片段
路径 B（标题/路径 LIKE 兜底）：title LIKE %词% OR path LIKE %词%
  └── 排除已在路径 A 命中的 id（去重）
```
- `MATCH '"q"*'`：前缀匹配（"错误"可命中"错误处理"）
- 中文限制：unicode61 单字切分下，**只保证 1-2 个字的短语匹配**；更长关键词需后续接入中文分词器（M2 优化项）

### 3. 排序策略
```
ORDER BY
  type 优先级：file > note > bookmark > todo > report > folder
  （文件结果优先，文件夹排最后——避免搜"错误"出现大量无关文件夹）
  → 标题精确命中（title LIKE）优先于内容命中
  → created_at 倒序
LIMIT 100
```

### 4. 前端错误处理（已修复的 bug）
- 症状：搜索"错误"曾显示 1170 条全量无关结果
- 根因：`onSearch` 的 `try/finally` **缺少 catch**——请求异常时静默保留上一列表（未搜索状态的全量数据）
- 修复：catch 中弹出「搜索失败」错误提示，不再误导为搜索结果

---

## 五、全局 UI 参数与规范

| 项 | 值 | 说明 |
|---|---|---|
| 全局主色 | `--kh-brand`（CSS 变量，= `--el-color-primary`） | 按钮强调蓝、文件标签背景统一取此变量 |
| 标签样式 | 主色底 + 白字，圆角 4px | 增强对比度（默认 `#409eff`） |
| 表格行高 | 29px | 标签列单行 + 溢出省略号 |
| 结构树行高 | 21px | `--el-tree-node-content-height` |
| 分页文案 | 共 N 条 / N条/页 / 前往 N 页 | Element Plus 全局 zh-cn locale |
| 主题 | 跟随系统 / 手动深色 / 手动浅色（三态） | 存 `html.dark` class，图表监听重渲染 |
| 滚动条 | 细滚动条（宽度约 6px） | 减少空间占用 |

---

## 六、运行方式

```bash
# 后端（5177）
cd server
npm run start        # 启动
npm run scan         # 手动全量扫描 storage_root

# 前端（5178）
cd web
npm run dev          # Vite 开发服务器（代理 /api → 127.0.0.1:5177）

# 或一键启动
start.bat
```

**配置**：`server/src/config.ts` 中 `STORAGE_ROOT` 指向主目录（当前为 `D:\编程开发\开源项目\knowledge-hub\Code`）；数据库位于 `server/data/knowledge.db`。

---

## 七、版本历史（近期）

| 提交 | 内容 |
|---|---|
| 5a6557e | 时间线 v4：贯穿线、日期行单独一行、折叠持久化、全部折叠/展开 |
| 4f10f01 | 报表中心：五类周期生成/编辑/导出/保存 |
| c112c96 | 五项优化：时间线标签筛选 + 按钮样式；行高 57→33px；主色参数 + 标签白字；分页中文化；搜索错误处理 |
| 30e2320 | 行高再压；结构树默认折叠 + 隐藏 Code 根；思维导图视图（图谱内）；功能说明文档 |
| 本轮 | **思维导图引擎替换**：simple-mind-map v0.14.0-fix.3（自研布局废弃）· 修复 SMM 字段映射（nodeData.data / generalization / associativeLineTargets uid 数组 / outerFrame / fillColor）· 概要/边界/关联线/颜色/形状/五布局/四主题全部真机验证通过 · 服务器 members 孤儿清理 · DB 脏数据重置 |

---

## 八、思维导图交互说明（帮助信息）

> 引擎：simple-mind-map v0.14.0-fix.3（MIT 开源）。导图数据存于 mindmaps / mindmap_nodes / mindmap_links / mindmap_members 四表。

### 1. 基本操作
| 操作 | 方式 | 说明 |
|---|---|---|
| 选中节点 | 单击 | 单击选中单个节点 |
| 多选节点 | Shift + 单击 | 可同时选中多个节点（用于概要/边界） |
| 编辑文字 | 双击节点 | 进入文字编辑 |
| 新建同级 | Enter | 在选中节点下方连续创建同级节点（可直接输入文字） |
| 新建子级 | Tab | 为选中节点创建子节点 |
| 删除节点 | Delete | 删除选中节点（含子树） |
| 调整层级 | 拖拽节点 | 拖到目标节点上方/下方/内部改变层级 |
| 全部修改 | 自动保存 | 操作后约 0.6 秒自动写入本地数据库 |

### 2. 关系线（两点间联系）
- **操作**：单击起点节点 → 点工具栏「关系」→ 再单击目标节点（**支持跨层级**，两个节点可以是任意层级）
- **语义**：在两节点之间建立一条**直线联系**，用于表达跨分支的关联（如"同步一致"）
- **样式**：细线（1.5px）+ 主题色虚线；**渲染在节点下层**，不会遮挡节点内容
- **文字**：双击关系线可编辑连线文字（如"同步一致"）

### 3. 概要（共同属性说明）
- **操作**：按住 Shift 选中**同一父节点的多个子节点** → 点工具栏「概要」→ 弹出多行输入框
- **语义**：概要显示在所选节点组上方，是描述这组节点**共同属性**的**长文本**（支持多行，如功能说明、规则描述）
- **约束**：只能针对同一父节点的多个子节点；已有概要的节点组不可重复添加

### 4. 边界（矩形分组）
- **操作**：选中一个父节点 → 点「边界」→ 输入边界名称 → **以矩形框包裹其所有子节点**；也可 Shift 多选同级节点框住所选
- **语义**：矩形显示在**底层**，用于把一组节点圈成功能分区（如"产品计划"下的功能/数据/界面）
- **文字**：双击边界文字可修改名称

### 5. 主题与配色
- 四套主题：Nexa 明亮 / Nexa 深色 / 经典分支 / 音蓝架构（右侧属性面板切换，随导图保存）
- **节点外观统一**：所有节点（根/二级/分支/概要）统一**圆角矩形**（圆角 10px），二级及以下节点**无边框实心块**，与父节点同款方格；层级差异仅体现在配色深浅
- **画布背景跟随系统主题**：系统深色模式（html.dark）下画布自动切换为深色背景（各主题内置 darkBg），并随系统主题实时联动（MutationObserver 监听 html.dark）；导图库卡片、编辑器工具栏/侧栏/输入框同样跟随系统深浅色
  - **实现要点**：SMM 重渲染（如切换布局）时会用 `themeConfig.backgroundColor`（**不是 `background`**）覆盖容器背景，因此主题配置必须同时提供 `background` 与 `backgroundColor` 两字段，深色适配时二者一并替换为 `darkBg`，否则切布局后背景会回落到 SMM 默认 #fafafa
- 节点颜色 / 形状：选中节点后可在右侧面板单独设置

### 6. 布局
- 五种布局：自由编辑 / 向右 / 向左 / 组织图 / 放射（左侧面板切换，随导图保存）

### 7. 界面与按钮规范
- **左侧不再显示快捷键文字提示**（原提示内容见本文档「八」各节，作为帮助信息保存）
- 顶部工具栏按钮**紧凑排列**（间距 4~6px）：＋子主题 / ＋同级 / 概要 / 边界 / 关系 为同款小按钮；返回、缩放（－/＋）为轻量文字按钮
- 关系线激活后**不显示两端拖拽锚点**（`enableAdjustAssociativeLinePoints: false`），只以高亮变色反馈选中状态

### 8. 已知说明
- 关联线为引擎原生贝塞尔曲线（非严格直线），短距离视觉近似直线；如需严格直线需改引擎源码（不建议）
- 导图库卡片显示节点数与最近编辑时间；删除导图后刷新列表即可

---

## 九、版本历史（续）

| 提交 | 内容 |
|---|---|
| 本轮 | **导图交互五修复**：①节点文字垂直居中（消除富文本 p 默认 margin 偏下溢出）②关联线细线 1.5px + 渲染底层（associativeLineIsAlwaysAboveNode=false）③概要/边界改为选中节点后操作（支持 Shift 多选、可跨层级），概要弹窗支持多行文本、边界弹窗输入名称 ④概要多行文本持久化（DB 存换行 + 渲染 pre-line）⑤Enter 连续同级、新建节点默认空文字（defaultInsertSecondLevelNodeText=''）、边界/关系线文字双击可编辑、分支节点与父节点同色系主题 |
| 本轮 | **导图 6 点 UI 优化**：①导图库卡片/编辑器/画布背景跟随系统深浅色（darkBg 各主题 + MutationObserver 联动 + JS 内联应用容器背景）②节点形状统一（根/二级/分支/概要统一圆角 10，二级去边框实心块，SMM 默认层级 borderRadius=5 需逐层覆盖）③关系线激活锚点隐藏（enableAdjustAssociativeLinePoints=false，renderControls 首行早退）④按钮风格统一 + 顶部间距收紧（toolbar 6px / tools 4px）⑤删除编辑器左侧快捷键文字提示（内容移入本文档八章）⑥工具栏紧凑布局 |
| 本轮 | **修复主题背景 bug**：切换布局后画布背景回落到 #fafafa —— 根因：SMM 重渲染用 `themeConfig.backgroundColor`（非 `background`）覆盖容器背景，主题配置缺该字段；修复为四套主题同时提供 `background` + `backgroundColor`，深色适配（themeCfgFor）时两字段一并替换为 darkBg |


## 十、思维导图进阶功能（2026-09-08）

### 1. 导图库管理（卡片 / 列表双视图）

- **视图切换**：顶部「卡片 / 列表」按钮切换。卡片视图展示标题 + 节点数 + 更新时间 + 标签；列表视图为单行紧凑行（同列信息）。
- **右键菜单**（卡片或列表行上右键）：置顶（📌，列表按置顶优先排序）/ 添加标签（逗号分隔，覆盖式）/ 重命名 / 删除（移入回收站）。
- **回收站**：删除不物理清除，而是软删除（deleted_at 打标）。回收站页显示「将在 X 前自动清理（保留 30 天）」提示，每条记录可「恢复」或「彻底删除」。恢复后自动回到导图库列表。

### 2. 全局删除约定（重点）

**软件内所有删除统一为「先移入回收站，超过 30 天自动清理」**：

1. 删除操作 → 记录打 deleted_at 标记（软删），列表/查询一律过滤；
2. 后台惰性清理：每次拉取列表时顺带删除超期（30 天）记录（级联清理其节点/关联线/成员表）；
3. 工作台显示清理信息横幅：回收站现有 N 项内容、将在何时前自动清理；
4. 用户可手动「恢复」或「彻底删除」。
- 实现：后端 mindmaps 表新增 pinned / tags / deleted_at 三列；路由增加 /recycle、/recycle/info、/:id/restore、/:id/permanent。

### 3. 大纲视图（左侧面板与布局设置共用区域）

- 左侧面板顶部「布局 / 大纲」Tab 切换，面板宽度可拖拽调整（右缘 2px 拖拽条）。
- 大纲树按层级缩进显示全部节点（含计数），行 hover 显示操作按钮：
  - ＋：添加子主题；⇥：添加同级（根节点除外）；✕：删除节点。
- 点击大纲行 = 选中画布对应节点（激活高亮，右侧出现节点颜色/形状面板）；双击可改名（弹窗）。
- 大纲行操作后画布实时同步，自动保存。

### 4. 导入与导出

- **导出**（工具栏「导出 ▾」下拉，9 种）：PNG 图片 / JPG 图片 / SVG 矢量图 / PDF 文档 / Markdown / 纯文本 / XMind / JSON 数据 / 思维导图工程文件(.smm)。导出即触发浏览器下载，文件名 = 导图标题。
- **导入**（工具栏「导入」按钮，支持 .mm / .csv / .txt / .md / .markdown / .json）：
  - Freemind（.mm）：解析 XML 层级；
  - CSV：路径（/ 或 \ 分隔）映射层级，可选备注列；
  - Markdown：标题（#）与列表（- * 1.）混合按缩进分层；
  - 文本（缩进式）：空格缩进决定层级；
  - JSON：导图数据结构直接恢复（自动补全 uid）。
  - 导入会**覆盖当前画布**（有确认提示），导入后自动保存。

### 5. 设置面板（右侧，原「文档字体与布局」合并）

- **布局**：自由编辑 / 向右 / 向左 / 组织图 / 放射，一键切换。
- **文档字号**：小（12）/ 标准（14）/ 大（16），持久化（localStorage kh-mm-fontsize），画布各级节点字号联动（根 +2 / 二级同 / 分支与概要 -1）。
- **文档主题**：Nexa 明亮 / Nexa 深色 / 经典分支 / 青蓝架构，以「可视化色块 + 名称」展示，一键切换。
- **节点颜色 / 形状**：单击画布节点后出现，可改节点填充色与 9 种形状（自动 / 矩形 / 圆角 / 椭圆 / 菱形 / 平行四边形 / 八角 / 外三角 / 内三角）。
- 主题跟随系统深浅色（html.dark 监听），也可在设置中手动切换。

> 修订说明：2026-09-08 第 2 轮迭代后，「文档字号」已移除；「布局」已并入左侧面板（见第十一章）。

## 十一、思维导图进阶迭代 2（2026-09-08）

### 1. 大纲视图：hover 按钮改为右键菜单

- 大纲行 hover 时不再显示 ＋/⇥/✕ 按钮（避免盖住内容），改为**右键菜单**（与导图库右键风格统一）。
- 右键菜单 4 项：**添加子主题 / 添加同级（根节点除外）/ 重命名 / 删除节点**；操作后画布实时同步并自动保存。
- 单击大纲行仍可选中并联动画布节点；双击仍可重命名。

### 2. 移除「文档字号」

- 该功能价值低、与系统主题体系冲突，已彻底移除（含 localStorage 键与字号叠加逻辑），不再提供。

### 3. 分支节点展开 / 折叠 + 折叠预览

- 所有有子节点的分支节点**常驻显示展开/折叠按钮**（`alwaysShowExpandBtn`），单击即折叠/展开该分支。
- **折叠预览（自研）**：折叠状态下，在节点下方自动显示被折叠的子节点名称列表（前 5 个，各截 14 字，用「／」连接），用户无需展开即可知道里面有什么。
- 实现：遍历 SMM 渲染树，对 `expand === false` 的节点在 SVG group 内动态插入 `foreignObject` 预览块；挂接 `node_tree_render_end` 与 `data_change`（350ms 防抖），主题切换 / 缩放 / 布局变化时自动重绘。

### 4. 布局与主题共用一个控件（左侧面板）

- 原右侧「布局 radio」行已删除；左侧「布局」页签内**并列布局（5 种）与主题色块（4 套）**，一体化切换。
- 右侧面板保留并改名为「设置」，仅含：节点颜色 / 节点形状 / 节点描述。

### 5. 导入改为下拉列表 + 导入到新文件

- 工具栏「导入 ▾」与导出一致，采用下拉列表 5 项：**Freemind(.mm) / CSV 表格 / Markdown / 文本（缩进） / JSON 数据**。
- **导入不再覆盖当前画布**：解析后自动创建一张新导图（文件名 = 原文件名去扩展名），写入数据库后直接打开新导图。
- 解析器统一为暂存根添加虚拟标记（`_virtual: true`），避免多根结构被当作真实节点入库。
- 下拉列表行高使用正常文字高度（全局紧凑行高约定）。

### 6. 幕布式节点描述（独立渲染层，自动换行）

- 单击节点后，右侧「设置」出现**描述输入框（多行 textarea）**；输入后自动保存。
- 描述显示在**节点标题下方**，多行自动换行（`word-break: break-all`），字号 11px 弱化显示，不遮挡节点正文。
- **技术要点（重要）**：描述不写入 SMM 富文本 `text`（simple-mind-map 的富文本序列化会把 HTML 转义/合并，污染标题），而是：
  1. 数据库 `mindmap_nodes.desc` 独立列存储（换行原样保留）；
  2. SMM 节点 `data.desc` 字段携带，标题保持纯文本；
  3. 渲染时在节点 group 下方自绘 `foreignObject` 描述块（自动换行、可随节点移动/缩放）；
  4. 编辑走 `setData({ desc })` + 重绘描述层，不触碰标题。
- 已兼容修复历史数据：早期版本误存进 `title` 的"标题+描述"HTML 会在加载时自动拆分回纯文本标题与描述。

### 7. 已知技术坑（供后续维护）

- **SMM 富文本序列化**：`SET_NODE_TEXT` 传入含 `<p>` 的 HTML 后，`getData()` 返回的 text 会被规范化（内层 `<p>` 被转义、多段 `<p>` 可能合并），因此"标题 + 描述"用富文本拼接的方案不可靠，必须用独立字段 + 独立渲染层。
- **折叠预览 / 描述层属于 SVG foreignObject**，不受 Vue scoped 样式作用，样式需写在全局（第二个非 scoped `<style>`）。
- 所有删除（导图/节点/文件等）遵循全局约定：软删 → 回收站 → 30 天后惰性自动清理，工作台显示清理提示。



---

## 第十二章 思维导图第 3 轮迭代（2026-09-08）

### 1. 界面布局调整

- **右侧「设置」面板已删除**，节点颜色 / 9 种形状 / 描述输入框合并到**左侧「布局」页签**的主题色块下方；单击画布节点后才显示（未选中时显示提示文字）。画布自动铺满原右侧区域。
- **布局切换改为按钮组**：左侧 5 个紧凑按钮（自由 / 向右 / 向左 / 组织 / 放射），两行排列，按钮间 2px 间距，文字为正常高度。
- **大纲高亮行**：选中行使用全局标签底色（--kh-brand #409eff）蓝底白字，与浏览页文件标签一致，替代原浅蓝半透明底。
- **导图库卡片置顶按钮**位于卡片**右上角**（top 8px / right 10px），不再遮挡左侧名称。

### 2. 右键菜单（导图库 + 大纲）

- 菜单使用 position: fixed + 鼠标视口坐标（clientX/Y），不再受滚动/容器定位影响——此前 absolute 定位在滚动后严重错位。
- 大纲行右键：添加子主题 / 添加同级 / 重命名 / 删除节点（删除进回收站）。

### 3. 折叠交互

- 分支节点折叠按钮**光标悬停节点时显示**（lwaysShowExpandBtn: false），不再常驻。
- **折叠预览已移除**：折叠节点下方不再显示子节点名预览条，画布更干净。

### 4. 手动保存（取消自动保存）

- 所有编辑操作（节点增删改、颜色/形状/描述、布局/主题、概要/边界/关系线）仅标记「未保存」状态，不再 600ms 自动落库。
- 工具栏新增**「保存」按钮**：有未保存修改时亮起（主题色），点击后全量保存（导图元信息 + 节点 + 关联线 + 边界/概要成员）。
- 状态提示：未修改 / 未保存 / 已保存 / 保存失败。
- 有未保存修改时**返回导图库**或**打开其它导图**，弹出确认框：「保存并返回 / 放弃修改」。

### 5. 折叠状态持久化（新增）

- mindmap_nodes 表新增 expand 列（1=展开，0=折叠），随保存全量写入；
- 重新打开导图时恢复各分支折叠状态，跨会话保持一致。

### 6. 放射（鱼骨）布局修复（依赖 simple-mind-map 源码补丁）

> ⚠️ 补丁位于 web/smm-patches/Fishbone.js（node_modules 被 git 忽略），重装依赖后需按
> web/smm-patches/README.md 恢复，并删除 web/node_modules/.vite 缓存后重启前端。

- **折叠跳位修复**：折叠中间分支后，右侧同级分支不再整体前移（如六顶思考帽折叠红/绿帽后黑帽位置保持不变）。原理：Fishbone.adjustLeftTopValue 新增模块级缓存 expandedWidthCache（uid → 最近一次展开渲染的子树水平宽度），折叠节点占位改用缓存宽度，兄弟节点 left 累加起点不再塌缩。
- **展开间距紧凑**：构造 MindMap 传 ishboneDeg: 58（默认 45°），斜线更陡、三级节点更贴近二级节点，横向占位明显减小（约压缩 1/4）。

## 第十三章 思维导图第 4 轮迭代（2026-09-08）

### 1. 节点描述渲染在节点内部（替代节点外浮层）

- 第 2 轮描述是节点矩形**下方**的独立浮层；本轮改为渲染在**节点矩形内部底部**。
- 实现基于 simple-mind-map 官方 `addCustomContentToNode` 钩子：
  - `create(node)`：读取 `node.nodeData.data.desc`，有描述时生成 `{ el, width, height }`（HTML 块，按行拆 div、自动换行），SMM 自动包成 foreignObject 追加进节点 group；无描述返回 null；
  - `handle({ content, element, node })`：渲染后把 foreignObject 平移到节点内底部（`node.height - 描述高 - 内边距`）；
  - **补丁 `nodeLayout.js` 的 `getNodeRect()`**：节点宽高计入追加内容尺寸（宽取 max(原宽, 描述宽+内边距)、高在原高上累加描述高）——否则描述会溢出节点矩形。补丁备份在 `web/smm-patches/nodeLayout.js`，与 Fishbone.js 一起按 README 恢复。
- `saveNodeDesc` 改为 `node.setData({ desc }) + node.reRender()`，描述编辑即时在节点内更新。
- 描述数据仍存独立字段 `mindmap_nodes.desc`，不写入 SMM 富文本标题（避免序列化污染）。

### 2. 放射（鱼骨）布局横向距离（继续压缩）

- `fishboneDeg`：58° → **72°**（分支斜线更陡，三级节点更贴近二级节点）；
- 放射布局下主题配置对二级节点 `marginX` 收紧（36 → 10）；
- 实测：二级节点 x=746、三级节点 x=896（SMM 逻辑坐标），**父子分支空隙约 70px**，较默认 45° 布局压缩约 2/3；
- 注：根节点→二级节点之间的主干长度由整棵子树高度决定（鱼骨布局固有），不随角度变化。

### 3. 布局按钮：加高 + 可视化图标

- 按钮 `padding: 7px`（高度约 28px，原 3px 太矮）；
- 每个布局前置内联 SVG 示意图标：自由=散点连线、向右=右向分支、向左=左向分支、组织=层级树、放射=鱼骨线（currentColor 跟随主题）。

### 4. 画布背景独立色板（幕布式）

- 左侧「主题」下方新增「画布背景」10 色色板：纯白 / 浅灰 / 米黄 / 浅蓝 / 浅绿 / 浅粉 / 深灰 / 深蓝灰 / 墨绿 / 深紫；
- 点击色板**覆盖主题背景**（themeConfig.background 与容器内联背景同步替换），并持久化到 `localStorage('kh-mm-canvasbg')`，刷新/重开保留；
- 设置了手动背景后，不再跟随系统深色自动切换（以手动选择为准）；
- 再次点击同色取消，恢复主题默认背景。

### 5. 导图库列表与卡片

- **卡片**：删除按钮 `✕` 移到**右下角**（hover 显示），与右上角置顶 📌 不再重叠；
- **列表**：文件名前新增思维导图类型图标（内联 SVG）；标签紧跟标题之后（不再被推到最右），节点数/更新时间右对齐——与浏览页"图标+名称+标签"风格统一。

### 6. 浏览页列表（资源管理器样式）

- 删除「类型」列：类型图标并入「名称」列，图标与文件名紧贴显示（类似 Windows 资源管理器列表模式）；
- 列头「标题」改为「名称」。

### 7. 帮助信息（面向最终用户）

- **节点描述**：选中节点后，在左侧「布局」页签的「描述」框中输入多行文字，即显示在节点矩形内部底部；用于补充说明、要点注释，不参与标题搜索。
- **画布背景**：左侧「画布背景」色板可独立调整画布底色，与主题互不冲突；系统深浅色切换时手动背景保持。
- **导图库**：卡片视图下删除按钮在右下角（悬停显示）；列表视图下文件前有导图图标，标签紧跟名称。
- **浏览页**：文件以「图标+名称」紧贴显示，不再单列类型。

## 第十四章 思维导图第 5 轮迭代（2026-09-08）

### 1. 加标签统一为「批量打标」弹窗

- 导图库右键「添加标签」与浏览页「批量打标」使用同一套弹窗：上方为已存在标签的多选列表（可点击删除标签），下方为「新标签输入框 + 新建按钮」。
- 选择后单击「确定」即**覆盖**该导图的全部标签；新建标签即时入库，无需先到别处创建。
- 目的：所有加标签入口风格统一、参数统一，避免同一个操作在不同界面出现不同形态（用户界面一致性原则）。

### 2. 高亮色全局统一（品牌蓝）

- 全局主色 `--kh-brand`（默认 `#409eff`，跟随 Element 主色）。
- 统一用于：浏览页表格选中行（蓝底白字）、导图库/大纲右键菜单悬停项（蓝底白字）、大纲选中行。
- 任何新列表/菜单的选中或悬停高亮都应复用 `--kh-brand`，不得另起相近色（避免深浅色下与背景混淆）。

### 3. 新建按钮支持导入

- 导图库工具栏「＋ 新建」改为下拉按钮：**新建导图** / **导入导图…**。
- 导入沿用下拉格式选择（Freemind / CSV / Markdown / 文本 / JSON），导入内容生成**新导图文件**，不覆盖当前打开文件。

### 4. 选中节点边框调细

- `.smm-hover-node`（悬停/选中描边）宽度固定为 `0.8px`（原默认较粗，视觉过重）。

### 5. 节点描述就地编辑（幕布式）

- 打开导图时传入 SMM 配置 `openRealtimeRenderOnNodeTextEdit: true`：
  - 双击节点文字进入编辑时，**编辑框透明无阴影**，原文本保持原位，输入内容实时渲染到节点——即「幕布」式的就地编辑，不再弹出悬浮白框。
- 富文本（Quill）模式下同样生效（`plugins/RichText.js` 已支持该配置：`box-shadow` 取消、背景透明）。
- 配合第 4 轮的「描述渲染在节点内部」，节点文字与描述都在节点矩形内完成查看与编辑。

### 6. 放射（鱼骨）布局 ——「子树紧凑拼接」算法（重要）

**需求背景**：旧鱼骨布局按固定角度（fishboneDeg）推算横向位置，分支多时主干被拉得很长、兄弟之间出现大量空白；且折叠某分支后兄弟节点会「跳位」（第 3 轮曾用 expandedWidthCache 占位修复，但角度方案本身仍有空白浪费）。用户否决「继续调角度」的路线，明确要求：**下一节点位置 = 上一节点（子树最长文字 + 留白）**。

**算法（基于 simple-mind-map 源码补丁，`web\smm-patches\Fishbone.js` 与 `fishboneUtils.js`）**：

- **二级节点**：分「上方 / 下方」两行（鱼骨形态），起点对齐根节点右侧；布局分两步：
  1. 初排：按自身宽度 + 留白横向排开；
  2. 精排（`adjustLeftTopValueAfter`）：按**子树最大水平宽度 + 10px 留白**逐兄弟推进——`width = getNodeBoundaries(item).right - left`，下一个兄弟 `left = 上一个兄弟的 left + 子树宽 + 10`。这就是「每个节点最长文字 + 留白 = 下一个节点位置」。
- **折叠占位**：折叠节点无子节点实例，用 `expandedWidthCache`（节点 uid → 最近一次展开时的子树宽度）占位，兄弟位置不受折叠影响（不跳位）。
- **三级及以下**：统一排在该父节点右侧 `父宽 + 10px` 处（替代原 `fishboneDeg` 角度公式），每层递进，横向紧凑；纵向仍按 getNodeAreaHeight 累计，保持鱼骨「上下翻转」的层级关系。
- **连线（renderLine）**：改为**父节点中心 → 子节点中心**直连（根节点为「主干水平线 + 到各二级节点中心斜线」）；每段线自行创建 path 对象（`this.lineDraw.path()` + `node.style.line(line)` + `node._lines.push(line)`），**不再依赖渲染器传入的 lines 数组**（原实现 `lines[index]` 在列式直连时会取到 undefined 而崩溃）。
- **总宽**：横向总宽 ≈ Σ（各二级子树宽 + 留白），一屏完整收纳；不再随分支数量无限拉长。

**补丁警告**：该算法改动 `simple-mind-map` 的 `src/layouts/Fishbone.js` 与 `src/layouts/fishboneUtils.js`（node_modules 内，npm install 后需按 `web\smm-patches\README.md` 恢复）。

### 7. 主题扩充与画布背景简化

- 画布背景**独立 10 色色板已删除**（不再需要单独选底色，避免与主题色重复/混淆）；画布背景跟随主题与系统深浅色。
- 主题扩至 **8 个**：Nexa 明亮 / Nexa 深色 / 经典分支 / 音蓝架构 / 莫兰迪 / 黑金 / 青碧 / 暖橙。
- 布局扩至 **8 个**：自由 / 向右 / 向左 / 导图 / 组织 / 放射 / 双鱼骨 / 时间轴（带可视化图标）。
- 布局与主题共用左侧同一面板；切换布局后自动收拢画布（fit）。

### 8. 无操作退出不弹「未保存」提示

- 打开导图后的 900ms 为「吸收期」：打开/初始化触发的内部改动（如布局渲染回填）不会标记脏状态；期间无用户操作直接返回，不弹出「有未保存的修改」确认框。
- 一旦用户实际改动（增删节点、改文字、切主题/布局等）立即恢复脏标记，退出仍会提示。

### 9. 导图库右键菜单位置修复

- 右键菜单改为 `position: fixed` 并直接取鼠标 `clientX/clientY`（限制在窗口内 160px 余量），不再减去容器偏移——菜单始终贴合鼠标，不再偏离。

### 帮助信息（面向最终用户，本节汇总）

- **就地编辑**：双击节点文字即可直接输入（无弹出白框），回车换行/完成。
- **放射布局**：上方/下方两行按「子树最长文字 + 留白」紧凑排布；折叠任意分支，其余兄弟不跳位。
- **主题与布局**：均在左侧面板切换；画布背景随主题与系统深浅色自动调整。
- **标签**：所有加标签操作（导图库右键、浏览页批量）使用同一弹窗，可新建、可删除、覆盖式保存。
- **右键菜单**：置顶 / 添加标签 / 重命名 / 删除（删除进入回收站，30 天后自动清理）。

## 第十五章 第 6 轮迭代（2026-09-08）：浏览页交互升级 + 放射布局回退精简

### 1. 浏览页：单击打开 + 文本预览/编辑

- **单击打开**：表格行/卡片单击即可——文件夹进入该目录，文本类文件（txt/md/代码等 24 种扩展名）弹出预览编辑框，图片弹出大图查看，其余类型提示不支持。
- **预览编辑框**（.txt/.md/代码类）：可编辑、可保存（PUT /api/file/:id 写回磁盘 + 更新 FTS 索引与 size）。
  - **md 特殊**：左右布局——左编辑、右实时渲染预览（自研极简渲染器：标题/列表/代码块/行内代码/链接/粗体/分割线，不引入重依赖）。
  - 保存按钮手动触发（与导图一致的"手动保存"哲学），保存后刷新列表。
- **后端安全**：保存接口只允许文本类扩展名（防二进制破坏）；GET/PUT 均校验路径防穿越（.. 拒绝、限定 storage_root 内）。

### 2. 浏览页：标签列与批量操作

- **标签列**：已有标签 → 只显示标签（蓝底白字 --kh-brand）；无标签 → 显示「加标签」小按钮（不再显示"打标"二字，语义统一）。
- **批量操作**（下拉，勾选后启用）：批量打标（原批量打标弹窗，含"包含子项"递归）/ **移动位置**（弹窗内选择目标文件夹，物理移动 + DB 路径/父子关系/子树路径前缀同步更新，防移动到自身）/ **加入待整理**（meta.pending=1，列表可筛选）。
- **待整理**：POST /api/resources/pending 批量标记，GET /api/resources?pending=1 筛选；字段存 resources.meta JSON（json_set 防非法 JSON）。

### 3. 刷新（重扫磁盘）

- 工具栏新增「刷新」：调 POST /api/rescan 增量扫描 storage_root。
- **增量扫描**（重写 scanner.ts）：不再 DELETE 全表重建——按 path 沿用已有记录 id（**标签关联不丢**）、新增磁盘条目插入、磁盘已删除记录清理；文本类扩展名读取内容进 FTS（上限 1MB），忽略 node_modules/.git 等。
- 解决"新增图片文件夹刷新不显示"：入库本是手动命令（npm run scan），现在 UI 一键刷新。

### 4. 放射布局：回退官方 + 紧凑化（重点）

- **背景**：第 5 轮"子树紧凑拼接 + 直连 path"patch 被实测否决（放射图线条方向错乱、XY 全不对）。
- **回退**：Fishbone.js、ishboneUtils.js 恢复官方 0.14.0-fix.3 原版（补丁已精简，见 web/smm-patches/README.md）。
- **保留的最小补丁**：
  1. Fishbone.js adjustLeftTopValue：去掉子树水平宽二次平移 → 二级节点间距 = 前兄弟自身宽 + marginX（实测 236px → 122px，六顶思考帽）；
  2. ishboneUtils.js adjustLeftTopValueAfter：只调整 top，不再用角度公式覆盖 left；
  3. 折叠节点占位缓存 expandedWidthCache（防折叠跳位）。
- **前端配合**：ishboneDeg: 72；	hemeCfgFor 放射分支 second.marginX: 6（官方默认 100 是横向空白主因）；**onLayoutChange 必须重设 themeConfig**（否则 marginX 覆盖不生效，本次已补）。
- **效果**：上下两排交替正确、连线方向正确、横向紧凑、无节点重叠（浅层树子节点垂直错开）。

### 5. 413 导入失败修复

- **根因**：server/src/index.ts 的 express.json() 默认 body 上限 **100kb**，导入较大 .mm（教育心理学【挖空背诵版】）时全量节点数组超限 → 413 PayloadTooLargeError。
- **修复**：express.json({ limit: '50mb' })（已用 3MB 请求体实测 200）。

### 6. 左侧面板样式

- **节点颜色**：16px 圆点（原 22px），末尾新增「默认」按钮（虚线圆+斜杠）：点击清除 fillColor/color，恢复主题自动色（curColor='' 表示跟随主题）。
- **主题行**：padding 6px 8px → 3px 7px，色块 22px → 16px，行高收紧。
- 布局按钮高度/图标保持第 4 轮风格（带可视化 SVG 图标）。

### 7. kity-dev 参考评估（未集成）

- D:\编程开发\开源项目\kity-dev 是百度 KityMinder 源码（bower+grunt 老式工程、基于 Kity 矢量库），与当前 simple-mind-map（Vue + ESM + 已有 6 轮迭代）**架构完全不兼容**，直接移植不现实。
- 结论：仅作交互参考（如键盘操作、节点操作面板），不集成；思维导图继续基于 simple-mind-map。

### 帮助信息（面向最终用户，本节补充）

- **浏览页**：单击打开文件/文件夹；txt/md/代码文件可在线编辑保存；md 左右分栏（左编辑右预览）。
- **刷新**：在 Code 目录新增/删除文件后点「刷新」即可入库（自动保留已有标签）。
- **加标签**：无标签的文件行显示「加标签」，点击同一弹窗（可新建/删除标签、文件夹可选递归）。
- **批量操作**：勾选后可用——批量打标 / 移动位置 / 加入待整理。
- **放射布局**：分支横向按文字长度紧凑排列，不再有大片空白；折叠分支不跳位。

---

## 十六、第 7 轮迭代：图谱 4 新图 · 独立编辑器页 · 框选 · 导图间距收敛 · 待整理入口

### 1. 图片文件图标与缩略图

- 图片类扩展名（png/jpg/jpeg/gif/webp/bmp/svg/tif/tiff/ico/avif）在浏览页表格与卡片中显示 🖼️ 图标（原误显示 📄 文本图标），卡片视图直接渲染缩略图（ileUrl(id) 懒加载）。
- py 等其他代码文件图标逻辑不变。

### 2. 图谱新增 4 种视图（数据层：文件夹层级 / 标签共现）

| 视图 | 数据来源 | 渲染 | 说明 |
| --- | --- | --- | --- |
| 矩形树图 | GET /api/graph/hierarchy（文件夹树，value=子树文件数、size=子树字节，自底向上累加） | ECharts treemap | 色块大小=文件数，层级嵌套 |
| 旭日图 | 同上 | ECharts sunburst | 环形层级，从根向外 |
| 打包图 | 同上（d3-hierarchy pack 布局） | ECharts custom 系列 | 气泡嵌套，父圆半透明+描边 |
| 弦图 | 标签维度数据反推共现 | ECharts chord | 同一资源两个标签连线，线宽=共现次数 |

- 打包图说明：ECharts 5.x/6.x 均无原生 pack 系列，故引入 d3-hierarchy 计算圆形布局，再用 custom 系列渲染；隐藏直角坐标轴（范围=画布尺寸）使 pi.coord 恒等转换；渲染上限 1500 个圆（全部展开时防卡顿）。
- 层级控制（全部展开/全部折叠/折叠到层）对这 4 种视图同样生效（剪枝到指定深度）。
- 依赖变更：echarts 6.1.0 → 5.6.0（兼容性验证：treemap/sunburst/sankey/tree/graph 等全部正常），新增 d3-hierarchy@3。

### 3. 独立编辑器页（替代预览弹窗）

- 浏览页单击 txt/md/代码/图片文件 → **整页切换**到 FileEditorView（App.vue editor 状态 v-if 全屏，非路由）。
- 顶栏：文件名 + 路径（左）、保存 / 关闭（右）；md 左右分栏等高（左编辑右实时渲染）；图片整页黑底 contain（无四周留白、无右下角文件名）；其他文本单编辑框。
- 关闭后自动回到浏览页并刷新列表（标签、新增文件等保持）。

### 4. 框选（浏览页表格/卡片 + 思维导图）

- **浏览页**：在列表/卡片区按住左键拖出半透明蓝色选区，松开后与选区相交的行或卡即选中（Ctrl 拖拽 = 追加到现有选择）；卡片左上角复选框可单选/多选；框选结束自动抑制随后的行单击（不会误打开文件）。
- **思维导图**：SMM 开启 useLeftKeySelectionRightKeyDrag: true，左键拖拽空白 = 框选多选（右键仍拖动画布）。

### 5. 思维导图第 7 轮调整

- 左侧面板「布局」tab 改名「**设置**」，tab 条冻结在面板顶部（sticky），面板内容独立滚动。
- **删除「自由」布局**：原 ree 与 导图 均映射 mindMap，完全重复 → 移除按钮与映射，保留 7 种：向右 / 向左 / 导图 / 组织 / 放射 / 双鱼骨 / 时间轴。
- **放射（鱼骨）横向间距**：second.marginX: 30（官方 100 过大、上轮 6 过窄致重叠），配合 ishboneDeg: 72，兄弟节点按"前兄弟宽度 + 30"定位，无重叠、无大空白。
- **纵向贴死根因修复**：SMM 布局纵向间距读取 themeConfig 的 second.marginY（第二层）/ 
ode.marginY（更深层）；自定义主题整体替换默认主题后 margin 键丢失 → 	hemeCfgFor() 显式补 second.marginY: 18、
ode.marginY: 24（教育心理学导图实测间距正常）。

### 6. 待整理入口

- 工具栏「待整理」按钮：激活后筛选 ?pending=1（仅显示标记待整理的资源），按钮变为 warning 色 + ✕ 点击退出。
- 批量操作下拉新增「移出待整理」，与「加入待整理」对称。

### 7. 报表页左右调换

- 已保存报表列表在左（窄列），报表内容编辑区在右（宽列），符合"列表-编辑"主次关系。

### 帮助信息（面向最终用户，本节补充）

- 图片/文本文件单击即整页打开编辑，不再弹窗；右上角保存/关闭。
- 列表或卡片上按住左键拖拽即可框选多个文件，配合批量操作使用。
- 图谱新增矩形树图/旭日图/打包图/弦图，展示文件夹层级与标签共现关系。
- 导图左侧「设置」页可调布局与主题；放射布局不再重叠、教育心理学类长文导图纵向不再贴死。
- 浏览页工具栏「待整理」可快速筛选待整理资源。

---

## 第十七章 第 8 轮迭代：MD 渲染修复、图谱下钻/多色/缩放、导图间距与动画、待整理递归去重

### 1. MD 预览空行修复

- 旧 renderMd 为"全局替换 + 每个换行变 <br/>"，空行产生双 <br> 导致预览大量空白。
- 重写为**逐行扫描渲染**：空行直接丢弃；标题 / 分隔线 / 列表（连续项合并一个 ul）/ 代码块 / 普通段落（多行合并为 p，行内用 <br/>）分别处理。
- 同时收窄 .fe-md-render 下 h1/h2/h3/p/li/pre 的 margin，预览密度接近常规 Markdown 文档。
- 实测：README.md 预览 br=0、p=5、标题=5、li=8，左右分栏等高、无空行。

### 2. 图谱（矩形树图/旭日图/打包图）下钻 + 配色 + 缩放

- **下钻（点击组节点放大）**：新增钻取栈 drillStack 与 zoomFactor；点击有子节点的分组 → 以该组为根重渲染（矩形树图走 ECharts 原生 nodeClick:'zoomToNode'；旭日/打包取点击项入栈重渲染）；点击空白区域逐级返回。
- 工具栏新增「返回上级」（钻取栈非空时可用）与「重置视图」（钻取栈/缩放非初始态时可用），重置恢复初始根与 100% 缩放；切换视图/重新加载自动重置。
- **旭日图多彩**：colorize 按"色相 = 210 + 层级*55 + 同级索引*47，明度 = 45 + 层级%4*9"逐层逐索引错色，不再整体一色。
- **打包图滚轮缩放**：监听画布 wheel 事件，zoomFactor 范围 0.4~3，坐标/半径以画布中心为基准统一缩放；文字显示阈值 r*z ≥ 16。
- **弦图**：改为 circular 圆周布局（节点沿圆周分布、中心连线，外观接近弦图）；每个节点独立配色（packPalette 轮转）；roam 支持滚轮缩放与拖拽平移。
- **统计修正**：层级视图总文件数不再递归重复累加（后端 value 已含子树文件数，顶层求和即总数）——Code 根从误显示 5126 修正为 1495。

### 3. 导图间距与动画

- **纵向间距减半**：second.marginY 18→9、node.marginY 24→12（教育心理学实测兄弟节点纵向间隔约 45px = 节点高 33 + 间距 12，不再贴死亦不过宽）。
- **关闭位置过渡动画**：transition: 0 + enableAnimation: false——打开/切换布局/展开折叠直接到位，消除动画中间帧"节点从中心重叠飞出、连线暂缺"的假性重叠/孤立观感（六顶思考帽 21 节点、教育心理学 543 节点均零重叠、连线完整）。

### 4. 待整理：递归子级 + 已标记跳过

- 加入待整理时：文件夹**递归其下全部子文件夹与文件**（BFS，seen 去重）；已标记待整理的资源自动跳过（skipped 计数）。
- 修复：文件夹自身已在待整理时，不再整个跳过子树——跳过自身（skipped++）但继续递归子项（子项可能未标记）。
- 接口返回 { count, skipped }；前端按结果组提示文案（"已加入 X 项（Y 项已待整理跳过）/ 文件夹含全部子项"）。
- 浏览页处于"待整理"筛选态时，批量操作菜单隐藏「加入待整理」项（避免把已待整理的再加一次）。
- 实测：再次加入 1Excel → count=494 skipped=1（494 个子项递归加入，自身跳过）。

### 帮助信息（面向最终用户，本节补充）

- MD 文件预览不再出现大片空行，与常规 Markdown 显示一致。
- 图谱矩形树图/旭日图/打包图：单击分组放大查看该组，点空白处逐级返回；打包图可滚轮缩放；旭日图/弦图已按层级或节点多色区分。
- 导图打开与切换布局瞬间完成（无飞行动画），展开分支后节点不重叠、连线完整；纵向行距较上一版减半。
- 把文件夹加入待整理时，其内部全部子文件与子文件夹会一并加入；已在待整理的不会重复加入。

---

## 第十八章 第 9 轮迭代：图谱标签开关/缩放/多色/打包嵌套、鱼骨图改名与水平间距、置顶、浏览返回与搜索合并

### 1. 图谱：全局「显示标签」开关（默认不显示）

- 工具栏新增「显示标签」勾选框，默认**不显示**（旭日图不再默认堆叠标签）。
- 开关统一控制**所有视图**（关系图/桑基图/树图/矩形树图/旭日图/打包图/弦图）的节点标签：showLabels 为 ref，watch 后立即重渲染当前视图。

### 2. 图谱：树图缩放与线宽、桑基图说明

- 树图加 `roam: true`（滚轮缩放 + 拖拽平移），连线加粗 width 1 → 1.8（原 1 过细）。
- 桑基图为 ECharts 原生系列，**官方不支持 roam**（5.x 系列无该配置，非本项目缺陷）；数据量通常较小，不缩放不影响使用；如需后续可按需改造为可缩放渲染。

### 3. 图谱：维度与视图绑定

- 矩形树图/旭日图/打包图基于文件夹层级（与维度无关）→ 切换到这些视图时「维度」radio **自动禁用**（灰色），避免"点击无反应"的困惑。
- 「全部展开/全部折叠/折叠到层」对所有视图生效（关系图/桑基图按深度过滤节点，树图折叠节点，层级图剪枝）。

### 4. 图谱：弦图连线多色

- 每条连线按**源节点颜色**独立着色（colorById 映射），不再整体一个颜色；线宽仍按共现次数。

### 5. 图谱：打包图父子嵌套观感 + 层级说明

- 父圆 fillOpacity 由 0.12~0.25 调整为**近透明（1级 0.05 / 深层 0.16）仅描边**，子圆清晰可见——嵌套层次分明（参照 G2 circle-packing 官方示例：https://g2.antv.antgroup.com/examples/graph/hierarchy/#circle-packing）。
- 「第 4/5 层显示一样」：Code 树实际最大深度约 5 级，剪枝到第 4 层与第 5 层时深层叶子差异极小，属数据深度限制（剪枝语义正确）。

### 6. 导图：鱼骨图（原放射）——还原官方版，只调水平 X 间距

- 经 git diff 核实：Fishbone.js / fishboneUtils.js 与第 6 轮"官方回退版"完全一致，**未残留任何紧凑补丁**（此前 marginX 30、marginY 减半、动画关闭均为前端 themeCfgFor / openMap 配置，未动布局源码）。
- 按用户第 4 次反馈要求：**只调整水平方向节点间 X 距离** → second.marginX 30 → **20**（官方 100 过大、6 过窄重叠、30 仍偏宽），fishboneDeg 72 保持。
- 六顶思考帽实测：节点无重叠、连线完整、横向留白收紧。

### 7. 导图：删除「双鱼骨」+「放射」改名「鱼骨图」

- 布局按钮删除「双鱼骨」（fishbone2 与鱼骨基本一样）；「放射」改名「**鱼骨图**」。
- LAYOUT_MAP / LAYOUT_ICONS / 打开导图布局校验列表同步清理；老数据存 fish2 回落为「向右」。
- 现布局共 6 种：向右 / 向左 / 导图 / 组织 / 鱼骨图 / 时间轴。

### 8. 置顶功能（浏览页）

- 后端：新增 `POST /api/resources/:id/pin`（meta.pinned 1/0，json_set 写入）。
- 排序：资源列表 ORDER BY 文件夹 → 置顶（pinned DESC）→ 所选排序字段（置顶不受排序方向影响）。
- 前端：列表行名称图标**前** + 卡片**右上角**显示单色浅色图钉图标（hover 品牌蓝；置顶后高亮 --kh-brand），单击切换置顶并刷新列表。
- 实测：1Excel 目录内置顶「操作TXT」后自动排最前。

### 9. 顶部页面切换标签间距

- `.main-tabs .el-tabs__item` padding 0 20px → **0 10px**，工作台/浏览/图谱/时间线/报表/导图间距收紧。

### 10. 浏览页：返回上一级 + 搜索合并

- **返回上一级**：当前目录非根时工具栏显示「← 返回」按钮（沿文件结构树查找当前文件夹父节点，逐级返回根目录；根目录下隐藏）。
- **搜索合并**：原「当前目录内按标题/路径筛选」与「全文搜索（FTS5）」两个输入框合并为**一个搜索框 + 范围下拉**：
  - 「文件名」：当前目录内 title/path 匹配（走资源列表 q 参数，含类型/标签/排序联动）；
  - 「内容」：FTS5 全库检索（跨 title/content，标题命中优先）。
  - 回车或点「搜索」触发；点「清除」恢复列表；实测按文件名（根目录搜"常用语句"=0，因该词在子目录中，属预期）与按内容（33 项命中）均正确。

### 帮助信息（面向最终用户，本节补充）

- 图谱：勾选「显示标签」可查看各视图节点名称；树图可滚轮缩放、连线更清晰；弦图连线按标签配色。
- 导图：布局按钮中「放射」已改名「鱼骨图」，双鱼骨已移除；鱼骨图横向更紧凑，节点不重叠。
- 浏览页：文件可单击图钉置顶（列表图标前 / 卡片右上角），置顶项始终排最前；子目录内可「← 返回」上一级；搜索一个框搞定——默认按文件名（当前目录），可切「内容」全库搜索。

---

## 第十九章 第 10 轮迭代：标签开关局部更新、旭日图渐进配色与深层修复、待整理入口按需显示、标签按文件夹重建、鱼骨图间距恢复

### 1. 图谱：切换「显示标签」不再重建图表

- 旧实现：watch(showLabels) → 整图 render（dispose + init）→ 关系图 force 布局重新收敛（形状变化）、树图重建（屏幕闪烁）。
- 新实现：**局部 setOption 只更新各视图 series 的 label.show**（`lazyUpdate` 合并更新）——布局与位置完全不动。
- 例外：打包图（custom 系列文字绘制在 renderItem 内，无 series 级 label）、弦图（标签在 data 项上）两种视图局部更新无效，仍走重渲染（无 force 重排，视觉无跳动）。

### 2. 浏览页：「待整理」入口按需显示

- 后端 `/api/stats` 增加 `pending` 计数（status=active 且 meta.pending=1 的资源数）。
- 工具栏「待整理」按钮 `v-if="pendingCount > 0 || pendingOnly"`：
  - **无待整理数据 → 按钮不显示**（正常浏览状态干净）；
  - 有待整理项 → 显示 `待整理（N）`；
  - 处于待整理筛选态 → 始终显示 `待整理 ✕` 以便退出。
- 刷新（重扫）后同步刷新计数。

### 3. 图谱：移除「返回上级」按钮

- 浏览页的「← 返回」是目录导航（逐级回父目录），图谱层级图（矩形树图/旭日图/打包图）的下钻返回**通过点击空白区域逐级返回**（第 8 轮已有逻辑）。
- 图谱工具栏只保留「重置视图」（一键回初始根 + 100% 缩放），避免两个「返回」概念混淆。

### 4. 旭日图：配色改为「同组渐进」

- 旧配色：每层每个节点错开色相 → 五颜六色、无层次。
- 新配色规则：
  - 根下第一层分组（顶级文件夹）：**同级错开色相**（色相 = 210 + 索引 × 47）；
  - 深层子分组/子对象：**继承父分组色相**，每层**明度 +9、饱和度 -10**（减淡），直至上限 78% / 下限 26%。
- 效果：组间颜色不同（可辨分组），组内逐层减淡（层次分明）。

### 5. 旭日图：圆环错位/重叠/缺口 —— 根因与修复

- **根因**：磁盘存在 6+ 层深目录（如 `5个人\1Excel\3工作表工作簿`、`8更多\VBA输入法收集`），旧 levels 只定义 3 层半径（18%–44%–70%–94%）；**第 4 层起的节点无半径定义，被 ECharts 渲染进浅层半径区** → 「显示在两个圆环中间」「与父级/叔级重叠」、圆环不完整。
- **修复**：levels 按数据最大深度**动态均分 18%→94%**（每层一个独立圆环带），任意深度节点落在自己的环带内。
- 已排除空文件夹（0 值）因素：库中无空文件夹。
- 说明：`5个人` 下确实存在一整套 `1Excel` 副本目录（用户个人资料），旭日图中是正常层级，非数据错误。

### 6. 标签体系重建（数据层）

- 背景：测试期对文件随意添加了 10 个标签 / 3630 条关联，导致标签维度（弦图、关系图标签模式、标签筛选）语义混乱。
- 操作（一次性脚本，已备份 `server/data/knowledge.db.bak-r10`）：
  1. 清空全部标签与关联；
  2. 按**顶级文件夹**重建 7 个标签：`1Excel / 2Word / 3Access / 4更多 / 5个人 / 6WPS / 图片`，每个标签**绑定该文件夹下全部递归子项**（文件夹 + 文件，共 1626 项）；
  3. 测试期残留的 288 条待整理标记清零（stats.pending=0）。
- 文件名的功能类标签（如「查找引用」「复制粘贴」等深层文件夹名）未自动建标签——需要时可按批量打标手动加，或明确规则后自动生成。

### 7. 鱼骨图：横向间距恢复 marginX 30

- 第 9 轮 marginX 30→20 后用户实测仍有重叠 → **恢复第 8 轮验收版 30**，不再调整；fishboneDeg 72 保持。
- 六顶思考帽实测：上下两行分支交替正确、节点无重叠、横向间距紧凑（兄弟节点约 30px 留白）。

### 帮助信息（面向最终用户，本节补充）

- 图谱「显示标签」开关：勾选/取消时图表不会跳动或重排，只显示/隐藏名称。
- 浏览页：没有待整理内容时工具栏不再显示「待整理」按钮；有待整理项时显示数量。
- 旭日图：各顶级文件夹颜色不同，内部子级逐层变浅；任意层级下钻放大后环带依旧完整、互不重叠。
- 鱼骨图布局回到之前的正常显示（横向留白 30）。
- 标签已按文件夹重建：每个顶级文件夹一个标签（1Excel/2Word/3Access/4更多/5个人/6WPS/图片），其下所有文件自动带上对应标签，可用于标签筛选与弦图分析。

---

## 第二十章 第 11 轮迭代：鱼骨图还原、待整理按状态显隐、弦图共现恢复（路径链标签）、旭日图剥离根

### 1. 鱼骨图：还原第 6 轮「最初正确版本」

- 横向留白 marginX 从 30 恢复为 **6**（第 6 轮验收版；官方默认 100 过大，30/20 均被用户实测仍有重叠）。
- 纵向留白 marginY 9/12 保持（第 8 轮按用户要求减半）。
- 实测：六顶思考帽、教育心理学【挖空背诵版】（543 节点）均无节点重叠、横向间距紧凑。

### 2. 待整理：按钮按筛选状态显示（不是去掉，是按状态启用）

- 批量操作菜单：
  - **非待整理筛选态**：仅显示「加入待整理」（勾选资源后加入）；
  - **待整理筛选态**：仅显示「移出待整理」（列表内均为待整理项，加入无意义）。
- 加入/移出操作后自动刷新待整理计数；工具栏「待整理（N）」入口按全局计数显隐（有待整理项才显示）。

### 3. 弦图空白根因与修复：标签体系改为「路径链」

- **根因**：上一轮标签重建为「每资源 1 个标签」→ 弦图按「同一资源上多个标签共现」建线 → 无共现 → 全空。
- **新标签体系（路径链）**：
  - 所有文件夹（不含根 Code）建为标签，共 **140 个**（同名文件夹加父路径区分，如 `1Excel` 与 `1Excel（5个人）`）；
  - 每个资源绑定其**路径上全部文件夹**的标签（如 `1Excel\单元格\合并转换\x.bas` → `1Excel`、`单元格`、`合并转换`），共 **4006 条**绑定；
  - 效果：弦图标签共现恢复（1766 节点 / 4006 连线）；标签语义 = 文件的完整文件夹分类，按标签筛选可精确定位到任意层级文件夹。
- 浏览页「按标签筛选」下拉增加可搜索（filterable），140 个标签可输入过滤。

### 4. 旭日图：剥离无意义的根（Code）

- 初始视图最内层直接显示**顶级文件夹**（1Excel/2Word/3Access/图片/4更多/5个人/6WPS），不再出现 Code 根；
- **配色从此层开始**：顶级文件夹同级错开色相，深层子级继承父色相逐层减淡（与第 10 轮配色规则一致）；
- 下钻、层级折叠后同样不出现 Code 根；
- 扇区分隔线变细（borderWidth 1.5 → 1）。

### 5. 常见疑问说明

- **旭日图 1Excel 与 2Word 之间的"缺口"**：是扇区之间的分隔线，且两者中间实际隔着 5个人、4更多、3Access 等扇区（按资源数降序排列），属正常分组边界，不是数据缺失。
- **为什么标签变多了**：路径链体系把每个文件夹都作为标签（含子文件夹），是为了让标签能表达"文件属于哪个分类"，并支撑弦图的共现分析；不需要时可忽略，筛选下拉支持搜索。

### 帮助信息（面向最终用户，本节补充）

- 待整理：在正常浏览时只能「加入待整理」；进入待整理视图后只能「移出待整理」——操作含义随视图自动切换。
- 弦图（标签维度）：现在展示文件夹分类之间的共现关系（同一文件同时属于多个分类时产生连线），不再空白。
- 旭日图：最内圈是顶级文件夹，外圈逐层是子文件夹；每个文件夹一种色系、内部逐层变浅。
