# Knowledge Hub 本地知识库 — 功能说明文档

> 版本：v0.6（2026-09-07）
> 项目位置：`D:\编程开发\开源项目\knowledge-hub`
> 技术栈：本地 Web 架构（Vue3 + Element Plus / Node.js + TypeScript / SQLite + FTS5），纯本地运行，无云端依赖。

---

## 一、整体架构

软件分为**两层 + 一个入口**：

```
┌─────────────────────────────────────────────┐
│  渲染层（前端 web/，Vite :5178）            │
│  工作台 · 浏览 · 图谱 · 时间线 · 报表        │
│  Element Plus 组件 + ECharts 图表            │
└──────────────────┬──────────────────────────┘
                   │ HTTP /api（axios 代理）
┌──────────────────▼──────────────────────────┐
│  业务层（后端 server/，Node :5177）          │
│  路由 routes/：resources · reports           │
│  规则：扫描、打标、搜索、报表周期计算        │
└──────────────────┬──────────────────────────┘
                   │ better-sqlite3
┌──────────────────▼──────────────────────────┐
│  数据层（SQLite knowledge.db）              │
│  resources / tags / resource_tags /         │
│  note_links / reports / attachments         │
│  + FTS5 全文索引（三触发器同步）            │
└─────────────────────────────────────────────┘
```

- **数据层**：所有实体（文件、文件夹、笔记、书签、待办、标签、报表）统一入库，每个资源带唯一 `id`（UUID）；标签通过 `resource_tags` 关联表多对多映射，与磁盘文件内容零耦合。
- **业务层**：保存/修改规则 + 界面操作执行规则（如"打标可递归到子文件"、"报表周期计算"、"搜索排序策略"）。
- **渲染层**：只负责取数、布局、交互与图表展示，不持有业务状态。

> 通用结论：任何软件都可抽象为"数据层 + 业务规则 + 渲染层"三层；本项目的价值在于把文件系统镜像成结构化数据库，再提供多维展示与批量操作。

---

## 二、数据表结构（SQLite）

| 表 | 用途 | 关键字段 |
|---|---|---|
| `resources` | 统一资源表（文件/文件夹/笔记/书签/待办/报表） | `id, type, title, path, parent_id, content, size, done, created_at, updated_at, status` |
| `tags` | 标签定义 | `id, name, color` |
| `resource_tags` | 资源↔标签多对多 | `resource_id, tag_id` |
| `note_links` | 笔记双链（引用维度，M5 后用） | `source_id, target_id` |
| `reports` | 已保存报表 | `id, title, type, content_md, content_html, period_start, period_end, created_at` |
| `attachments` | 附件 | 预留 |
| `resources_fts` | FTS5 全文索引（虚拟表） | `title, content`，由 `resources_ai/ad/au` 三个触发器在插入/删除/更新时自动同步 |

**关键设计**：文件/文件夹是磁盘扫描的镜像（`scanner` 增量扫描），**标签是独立于磁盘的本地元数据**——标签状态丢失不影响文件内容，重扫/改名均不触碰标签（仅需在扫描时保留 id 映射）。

---

## 三、核心功能模块

### 1. 工作台
- 顶部统计：资源总数 / 文件数 / 文件夹数（类型分布）
- 常用标签卡片（按使用次数 TOP10，点击跳转浏览页按该标签过滤）
- 顶层目录卡片（文件数/子目录数/占用空间）
- 最近更新文件列表

### 2. 浏览（= 原"浏览 + 数据库"合并页）
- 左侧**文件结构树**：Code 主目录下逐级目录树；默认全部折叠；支持全部展开/全部折叠；行高 21px
- 右侧**表格 / 卡片**双视图切换：
  - 表格：类型 / 标题 / 路径 / 标签 / 大小 / 更新时间，行高 29px，标签统一**主色蓝底白字**
  - 卡片：缩略图（图片类文件预览）/ 图标 + 标题 + 元信息 + 标签
- 筛选：标题/路径关键字、类型、标签、更新时间排序；**全文搜索（FTS5）**
- **打标**：单文件/批量打标（可勾选"同时应用到所有子文件和子文件夹"递归）；弹窗内可直接新建标签
- 分页：共 N 条 / 每页 N 条 / 前往第 N 页（全中文）

### 3. 图谱
- **三维度**：文件夹 / 标签 / 引用（双链，M5 后可用）
- **四视图**：
  - **关系图**：力导向布局，可拖拽，动画已关闭
  - **桑基图**：层级流向
  - **树图**：纵向层级树
  - **思维导图**：横向布局（根在左），节点按文件夹/文件着色，**点击节点折叠/展开，双击节点弹出打标对话框（即编辑）**
- **层级控制（所有视图通用）**：全部展开 / 全部折叠 / 折叠到第 N 层
  - 层数语义：**第 1 层 = 全部折叠（根 + 一级节点）**；第 N 层 = 显示到第 N 级；上限 = 树层级数 - 1
  - 实现：树图/思维导图用数据剪枝（`pruneTree`）+ 全展开，规避 ECharts `initialTreeDepth` 的层数偏移 bug；多根森林包一层隐藏虚拟根 `__forest__`
- **主题**：图表颜色随系统深色/浅色自动切换（监听 `kh-theme-change` 事件重渲染）

### 4. 时间线（垂直时间线）
- 按创建时间倒序分组（每天一组），左侧贯穿时间线 + 圆点 + 箭头
- 每条显示：名称 / 标签 / 时间或大小
- **筛选**：类型下拉 + **标签下拉**（含使用次数，`Access（163）` 格式），可清空
- **折叠**：每天一组可单独折叠（持久化到 sessionStorage）；顶部「全部折叠 / 全部展开」按钮（带边框方格样式，紧跟筛选）
- 行高紧凑；筛选行固定在页面顶部

### 5. 报表中心
- 五类周期：日报 / 周报 / 月报 / 季报 / 年报（按资源 `created_at` 落入统计区间计算）
- 报表内容：周期内新增资源明细 + 热门标签排行 + 类型分布
- 可编辑（Markdown）、导出（.md / .html）、保存（写入 `resources` type='report' + `reports` 表）
- 右侧已保存报表列表

---

### 6. 思维导图（独立导图工作区，参考 NexaNote 织记）
- **引擎**：**simple-mind-map v0.14.0-fix.3**（国产 MIT 开源引擎，已 `npm install`；组织图/概要/边界/关联线/节点样式/多选拖拽全部内置）。此前自研布局（ECharts tree / 自由坐标算法）被用户多次否决，最终切换到成熟引擎。NexaNote-main 源码（Flutter+FastAPI 手写笔记应用）经核查无思维导图代码，不可移植。
- **导图库**：多张导图管理（新建/打开/删除，显示节点数与更新时间）
- **编辑器**（三栏布局，对齐 NexaNote 界面）：
  - 顶部工具栏：＋子主题 / ＋同级 / 概要 / 边界 / 关系 / 缩放（40%~200%）/ 已保存状态
  - 左侧布局面板：**自由编辑 / 向右 / 向左 / 组织图 / 放射**（快捷键与多选等操作提示已移入本文档「八、思维导图交互说明」，界面不再显示文字提示）
  - 右侧节点属性：**文档主题**（Nexa 明亮 / Nexa 深色 / 经典分支 / 音蓝架构）、节点颜色（6 色覆盖，写 `data.fillColor`）、节点形状（自动/矩形/圆角/椭圆，写 `data.shape`）
- **编辑能力**：单击选中（右侧面板联动）、双击改名、Enter 同级、Tab 子级、Delete 级联删除、自由布局拖拽、概要（`ADD_GENERALIZATION`，数据 `data.generalization`）、边界（自定义整体矩形，见下）、关联线（`ADD_ASSOCIATIVE_LINE`，数据为 `data.associativeLineTargets` **uid 字符串数组**）
- **多选才可用的三按钮**：工具栏「概要 / 边界 / 关系」必须**选中 ≥2 个节点**才可点击（单选时置灰禁用），通过 Shift 多选或按住选择实现；"选中 2 个节点"是三者共同的交互前提
- **概要（多行直接编辑）**：点「概要」按钮后**不弹窗**，直接在节点内进入可编辑文本框（contenteditable），可输入**多行文本**；回车换行、失焦/回车提交并自动保存
  - 多行实现要点：概要文本在 SMM 数据中必须是**多个 `<p>` 段**（`<p>行1</p><p>行2</p>`），纯文本 `\n` 会被 SMM `htmlEscape` 吞掉换行；DB 存储用 `\n` 分隔（`</p>`→`\n` 转换），载入时按 `\n` 拆成多 `<p>`
- **边界（整体矩形语义，已否决 SMM 原生 outerFrame）**：多选 ≥2 个节点后点「边界」，在**这些节点的外围创建一个整体虚线矩形**包裹（不是给每个节点单独挂框）；矩形可单击选中（红边高亮）、**双击文字改名**、Delete 删除；矩形与节点同层，位于节点之下不遮挡内容
- **关系线（底层可点选）**：多选 ≥2 个节点后点「关系」创建（选中顺序即方向，先选为起点）；线绘制在**节点底层**（`associativeLineIsAlwaysAboveNode: false`，不盖节点内容）；**单击线可选中**（高亮变蓝）、**双击线中文字可内联编辑**（出现白底输入框）、Delete 删除
  - 可点选实现要点：SMM 的点击热区是**透明描边 path**，SVG 默认 `pointer-events` 不命中透明描边，需 CSS 强制 `.smm-associative-line-container path { pointer-events: stroke }`，且线文字 `pointer-events: none` 穿透到热区
- **自动保存**：`data_change`/`view_data_change` → 600ms 防抖 → 全量保存（nodes/links/members 四请求并行；服务器端 members 先删本图组员+清理孤儿组员再插）
- **数据模型**：`mindmaps`（导图，layout 存 free/right/left/org/radial）+ `mindmap_nodes`（节点，kind=node/boundary/summary，含颜色/形状）+ `mindmap_links`（关系连线 source→target）+ `mindmap_members`（边界/概要组员 group_id→node_id）
- **关键映射细节**（SMM 字段权威，勿凭记忆）：
  - SMM Node 实例：`node.getData('text'|'uid'|'fillColor'|'shape')`；`node.nodeData.data` 为数据体；虚拟根 `__VR__` 判断用 uid
  - 概要：DB summary（kind=summary, parent=所属节点）→ 载入挂 `data.generalization=[{text:'<p>…</p><p>…</p>', richText:true}]`；保存读 `data.generalization`，多 `<p>` 转 `\n`
  - 边界：**自定义 khBounds 运行时层**（DB boundary + members 构建，不向 SMM data 挂 outerFrame）；渲染时在 `.smm-container` svg 内插 `g.kh-bound-layer`（首个子元素、节点之下），用 `node.getRect()` 取成员包围盒 + padding 14 画虚线矩形 + 左上角名称文字；无边界时**必须移除空 `<g>`**（空 g 会让 SMM Scrollbar `rbox()` 崩溃致整图空白）；单击矩形选中、双击文字改名、Delete 删除，smmToDb 时写回 nodes(boundary 行)+members
  - 关联线：`data.associativeLineTargets` 是 **uid 字符串数组**（非对象数组）；节点激活事件为 `node_active`（v0.14 无 `node_click`）；`INSERT_CHILD_NODE(openEdit, [node])` 首参是 openEdit 布尔；激活的线通过 `clickPath` 变蓝显示（无 class 标记）
  - 旧 DB 数据 title 为双重转义 HTML（`&lt;p&gt;111&lt;/p&gt;`），载入过 `plain()` 解码剥离标签
- **图谱页与导图页分离**：图谱页（关系图/桑基图/树图）**不再包含思维导图入口**；思维导图仅在顶部「导图」页使用

---

## 四、搜索原理（重点）

### 1. 索引层：SQLite FTS5
- 建表 `resources_fts(title, content)`，tokenizer 为 `unicode61`（英文按词切分、中文按**单字**切分）
- 三个触发器自动同步：
  - `resources_ai`（INSERT 后）：新资源写入索引
  - `resources_ad`（DELETE 后）：删除索引条目
  - `resources_au`（UPDATE 后）：先删旧再插新（**改名/改内容后搜索立即生效**）

### 2. 查询层：双路合并
```
路径 A（全文命中）：resources_fts MATCH '"关键词"*'
  └── JOIN resources 取元数据，snippet() 生成高亮片段
路径 B（标题/路径 LIKE 兜底）：title LIKE %词% OR path LIKE %词%
  └── 排除已在路径 A 命中的 id（去重）
```
- `MATCH '"q"*'`：前缀匹配（"错误"可命中"错误处理"）
- 中文限制：unicode61 单字切分下，**只保证 1-2 个字的短语匹配**；更长关键词需后续接入中文分词器（M2 优化项）

### 3. 排序策略
```
ORDER BY
  type 优先级：file > note > bookmark > todo > report > folder
  （文件结果优先，文件夹排最后——避免搜"错误"出现大量无关文件夹）
  → 标题精确命中（title LIKE）优先于内容命中
  → created_at 倒序
LIMIT 100
```

### 4. 前端错误处理（已修复的 bug）
- 症状：搜索"错误"曾显示 1170 条全量无关结果
- 根因：`onSearch` 的 `try/finally` **缺少 catch**——请求异常时静默保留上一列表（未搜索状态的全量数据）
- 修复：catch 中弹出「搜索失败」错误提示，不再误导为搜索结果

---

## 五、全局 UI 参数与规范

| 项 | 值 | 说明 |
|---|---|---|
| 全局主色 | `--kh-brand`（CSS 变量，= `--el-color-primary`） | 按钮强调蓝、文件标签背景统一取此变量 |
| 标签样式 | 主色底 + 白字，圆角 4px | 增强对比度（默认 `#409eff`） |
| 表格行高 | 29px | 标签列单行 + 溢出省略号 |
| 结构树行高 | 21px | `--el-tree-node-content-height` |
| 分页文案 | 共 N 条 / N条/页 / 前往 N 页 | Element Plus 全局 zh-cn locale |
| 主题 | 跟随系统 / 手动深色 / 手动浅色（三态） | 存 `html.dark` class，图表监听重渲染 |
| 滚动条 | 细滚动条（宽度约 6px） | 减少空间占用 |

---

## 六、运行方式

```bash
# 后端（5177）
cd server
npm run start        # 启动
npm run scan         # 手动全量扫描 storage_root

# 前端（5178）
cd web
npm run dev          # Vite 开发服务器（代理 /api → 127.0.0.1:5177）

# 或一键启动
start.bat
```

**配置**：`server/src/config.ts` 中 `STORAGE_ROOT` 指向主目录（当前为 `D:\编程开发\开源项目\knowledge-hub\Code`）；数据库位于 `server/data/knowledge.db`。

---

## 七、版本历史（近期）

| 提交 | 内容 |
|---|---|
| 5a6557e | 时间线 v4：贯穿线、日期行单独一行、折叠持久化、全部折叠/展开 |
| 4f10f01 | 报表中心：五类周期生成/编辑/导出/保存 |
| c112c96 | 五项优化：时间线标签筛选 + 按钮样式；行高 57→33px；主色参数 + 标签白字；分页中文化；搜索错误处理 |
| 30e2320 | 行高再压；结构树默认折叠 + 隐藏 Code 根；思维导图视图（图谱内）；功能说明文档 |
| 本轮 | **思维导图引擎替换**：simple-mind-map v0.14.0-fix.3（自研布局废弃）· 修复 SMM 字段映射（nodeData.data / generalization / associativeLineTargets uid 数组 / outerFrame / fillColor）· 概要/边界/关联线/颜色/形状/五布局/四主题全部真机验证通过 · 服务器 members 孤儿清理 · DB 脏数据重置 |

---

## 八、思维导图交互说明（帮助信息）

> 引擎：simple-mind-map v0.14.0-fix.3（MIT 开源）。导图数据存于 mindmaps / mindmap_nodes / mindmap_links / mindmap_members 四表。

### 1. 基本操作
| 操作 | 方式 | 说明 |
|---|---|---|
| 选中节点 | 单击 | 单击选中单个节点 |
| 多选节点 | Shift + 单击 | 可同时选中多个节点（用于概要/边界） |
| 编辑文字 | 双击节点 | 进入文字编辑 |
| 新建同级 | Enter | 在选中节点下方连续创建同级节点（可直接输入文字） |
| 新建子级 | Tab | 为选中节点创建子节点 |
| 删除节点 | Delete | 删除选中节点（含子树） |
| 调整层级 | 拖拽节点 | 拖到目标节点上方/下方/内部改变层级 |
| 全部修改 | 自动保存 | 操作后约 0.6 秒自动写入本地数据库 |

### 2. 关系线（两点间联系）
- **操作**：单击起点节点 → 点工具栏「关系」→ 再单击目标节点（**支持跨层级**，两个节点可以是任意层级）
- **语义**：在两节点之间建立一条**直线联系**，用于表达跨分支的关联（如"同步一致"）
- **样式**：细线（1.5px）+ 主题色虚线；**渲染在节点下层**，不会遮挡节点内容
- **文字**：双击关系线可编辑连线文字（如"同步一致"）

### 3. 概要（共同属性说明）
- **操作**：按住 Shift 选中**同一父节点的多个子节点** → 点工具栏「概要」→ 弹出多行输入框
- **语义**：概要显示在所选节点组上方，是描述这组节点**共同属性**的**长文本**（支持多行，如功能说明、规则描述）
- **约束**：只能针对同一父节点的多个子节点；已有概要的节点组不可重复添加

### 4. 边界（矩形分组）
- **操作**：选中一个父节点 → 点「边界」→ 输入边界名称 → **以矩形框包裹其所有子节点**；也可 Shift 多选同级节点框住所选
- **语义**：矩形显示在**底层**，用于把一组节点圈成功能分区（如"产品计划"下的功能/数据/界面）
- **文字**：双击边界文字可修改名称

### 5. 主题与配色
- 四套主题：Nexa 明亮 / Nexa 深色 / 经典分支 / 音蓝架构（右侧属性面板切换，随导图保存）
- **节点外观统一**：所有节点（根/二级/分支/概要）统一**圆角矩形**（圆角 10px），二级及以下节点**无边框实心块**，与父节点同款方格；层级差异仅体现在配色深浅
- **画布背景跟随系统主题**：系统深色模式（html.dark）下画布自动切换为深色背景（各主题内置 darkBg），并随系统主题实时联动（MutationObserver 监听 html.dark）；导图库卡片、编辑器工具栏/侧栏/输入框同样跟随系统深浅色
  - **实现要点**：SMM 重渲染（如切换布局）时会用 `themeConfig.backgroundColor`（**不是 `background`**）覆盖容器背景，因此主题配置必须同时提供 `background` 与 `backgroundColor` 两字段，深色适配时二者一并替换为 `darkBg`，否则切布局后背景会回落到 SMM 默认 #fafafa
- 节点颜色 / 形状：选中节点后可在右侧面板单独设置

### 6. 布局
- 五种布局：自由编辑 / 向右 / 向左 / 组织图 / 放射（左侧面板切换，随导图保存）

### 7. 界面与按钮规范
- **左侧不再显示快捷键文字提示**（原提示内容见本文档「八」各节，作为帮助信息保存）
- 顶部工具栏按钮**紧凑排列**（间距 4~6px）：＋子主题 / ＋同级 / 概要 / 边界 / 关系 为同款小按钮；返回、缩放（－/＋）为轻量文字按钮
- 关系线激活后**不显示两端拖拽锚点**（`enableAdjustAssociativeLinePoints: false`），只以高亮变色反馈选中状态

### 8. 已知说明
- 关联线为引擎原生贝塞尔曲线（非严格直线），短距离视觉近似直线；如需严格直线需改引擎源码（不建议）
- 导图库卡片显示节点数与最近编辑时间；删除导图后刷新列表即可

---

## 九、版本历史（续）

| 提交 | 内容 |
|---|---|
| 本轮 | **导图交互五修复**：①节点文字垂直居中（消除富文本 p 默认 margin 偏下溢出）②关联线细线 1.5px + 渲染底层（associativeLineIsAlwaysAboveNode=false）③概要/边界改为选中节点后操作（支持 Shift 多选、可跨层级），概要弹窗支持多行文本、边界弹窗输入名称 ④概要多行文本持久化（DB 存换行 + 渲染 pre-line）⑤Enter 连续同级、新建节点默认空文字（defaultInsertSecondLevelNodeText=''）、边界/关系线文字双击可编辑、分支节点与父节点同色系主题 |
| 本轮 | **导图 6 点 UI 优化**：①导图库卡片/编辑器/画布背景跟随系统深浅色（darkBg 各主题 + MutationObserver 联动 + JS 内联应用容器背景）②节点形状统一（根/二级/分支/概要统一圆角 10，二级去边框实心块，SMM 默认层级 borderRadius=5 需逐层覆盖）③关系线激活锚点隐藏（enableAdjustAssociativeLinePoints=false，renderControls 首行早退）④按钮风格统一 + 顶部间距收紧（toolbar 6px / tools 4px）⑤删除编辑器左侧快捷键文字提示（内容移入本文档八章）⑥工具栏紧凑布局 |
| 本轮 | **修复主题背景 bug**：切换布局后画布背景回落到 #fafafa —— 根因：SMM 重渲染用 `themeConfig.backgroundColor`（非 `background`）覆盖容器背景，主题配置缺该字段；修复为四套主题同时提供 `background` + `backgroundColor`，深色适配（themeCfgFor）时两字段一并替换为 darkBg |


## 十、思维导图进阶功能（2026-09-08）

### 1. 导图库管理（卡片 / 列表双视图）

- **视图切换**：顶部「卡片 / 列表」按钮切换。卡片视图展示标题 + 节点数 + 更新时间 + 标签；列表视图为单行紧凑行（同列信息）。
- **右键菜单**（卡片或列表行上右键）：置顶（📌，列表按置顶优先排序）/ 添加标签（逗号分隔，覆盖式）/ 重命名 / 删除（移入回收站）。
- **回收站**：删除不物理清除，而是软删除（deleted_at 打标）。回收站页显示「将在 X 前自动清理（保留 30 天）」提示，每条记录可「恢复」或「彻底删除」。恢复后自动回到导图库列表。

### 2. 全局删除约定（重点）

**软件内所有删除统一为「先移入回收站，超过 30 天自动清理」**：

1. 删除操作 → 记录打 deleted_at 标记（软删），列表/查询一律过滤；
2. 后台惰性清理：每次拉取列表时顺带删除超期（30 天）记录（级联清理其节点/关联线/成员表）；
3. 工作台显示清理信息横幅：回收站现有 N 项内容、将在何时前自动清理；
4. 用户可手动「恢复」或「彻底删除」。
- 实现：后端 mindmaps 表新增 pinned / tags / deleted_at 三列；路由增加 /recycle、/recycle/info、/:id/restore、/:id/permanent。

### 3. 大纲视图（左侧面板与布局设置共用区域）

- 左侧面板顶部「布局 / 大纲」Tab 切换，面板宽度可拖拽调整（右缘 2px 拖拽条）。
- 大纲树按层级缩进显示全部节点（含计数），行 hover 显示操作按钮：
  - ＋：添加子主题；⇥：添加同级（根节点除外）；✕：删除节点。
- 点击大纲行 = 选中画布对应节点（激活高亮，右侧出现节点颜色/形状面板）；双击可改名（弹窗）。
- 大纲行操作后画布实时同步，自动保存。

### 4. 导入与导出

- **导出**（工具栏「导出 ▾」下拉，9 种）：PNG 图片 / JPG 图片 / SVG 矢量图 / PDF 文档 / Markdown / 纯文本 / XMind / JSON 数据 / 思维导图工程文件(.smm)。导出即触发浏览器下载，文件名 = 导图标题。
- **导入**（工具栏「导入」按钮，支持 .mm / .csv / .txt / .md / .markdown / .json）：
  - Freemind（.mm）：解析 XML 层级；
  - CSV：路径（/ 或 \ 分隔）映射层级，可选备注列；
  - Markdown：标题（#）与列表（- * 1.）混合按缩进分层；
  - 文本（缩进式）：空格缩进决定层级；
  - JSON：导图数据结构直接恢复（自动补全 uid）。
  - 导入会**覆盖当前画布**（有确认提示），导入后自动保存。

### 5. 设置面板（右侧，原「文档字体与布局」合并）

- **布局**：自由编辑 / 向右 / 向左 / 组织图 / 放射，一键切换。
- **文档字号**：小（12）/ 标准（14）/ 大（16），持久化（localStorage kh-mm-fontsize），画布各级节点字号联动（根 +2 / 二级同 / 分支与概要 -1）。
- **文档主题**：Nexa 明亮 / Nexa 深色 / 经典分支 / 青蓝架构，以「可视化色块 + 名称」展示，一键切换。
- **节点颜色 / 形状**：单击画布节点后出现，可改节点填充色与 9 种形状（自动 / 矩形 / 圆角 / 椭圆 / 菱形 / 平行四边形 / 八角 / 外三角 / 内三角）。
- 主题跟随系统深浅色（html.dark 监听），也可在设置中手动切换。

> 修订说明：2026-09-08 第 2 轮迭代后，「文档字号」已移除；「布局」已并入左侧面板（见第十一章）。

## 十一、思维导图进阶迭代 2（2026-09-08）

### 1. 大纲视图：hover 按钮改为右键菜单

- 大纲行 hover 时不再显示 ＋/⇥/✕ 按钮（避免盖住内容），改为**右键菜单**（与导图库右键风格统一）。
- 右键菜单 4 项：**添加子主题 / 添加同级（根节点除外）/ 重命名 / 删除节点**；操作后画布实时同步并自动保存。
- 单击大纲行仍可选中并联动画布节点；双击仍可重命名。

### 2. 移除「文档字号」

- 该功能价值低、与系统主题体系冲突，已彻底移除（含 localStorage 键与字号叠加逻辑），不再提供。

### 3. 分支节点展开 / 折叠 + 折叠预览

- 所有有子节点的分支节点**常驻显示展开/折叠按钮**（`alwaysShowExpandBtn`），单击即折叠/展开该分支。
- **折叠预览（自研）**：折叠状态下，在节点下方自动显示被折叠的子节点名称列表（前 5 个，各截 14 字，用「／」连接），用户无需展开即可知道里面有什么。
- 实现：遍历 SMM 渲染树，对 `expand === false` 的节点在 SVG group 内动态插入 `foreignObject` 预览块；挂接 `node_tree_render_end` 与 `data_change`（350ms 防抖），主题切换 / 缩放 / 布局变化时自动重绘。

### 4. 布局与主题共用一个控件（左侧面板）

- 原右侧「布局 radio」行已删除；左侧「布局」页签内**并列布局（5 种）与主题色块（4 套）**，一体化切换。
- 右侧面板保留并改名为「设置」，仅含：节点颜色 / 节点形状 / 节点描述。

### 5. 导入改为下拉列表 + 导入到新文件

- 工具栏「导入 ▾」与导出一致，采用下拉列表 5 项：**Freemind(.mm) / CSV 表格 / Markdown / 文本（缩进） / JSON 数据**。
- **导入不再覆盖当前画布**：解析后自动创建一张新导图（文件名 = 原文件名去扩展名），写入数据库后直接打开新导图。
- 解析器统一为暂存根添加虚拟标记（`_virtual: true`），避免多根结构被当作真实节点入库。
- 下拉列表行高使用正常文字高度（全局紧凑行高约定）。

### 6. 幕布式节点描述（独立渲染层，自动换行）

- 单击节点后，右侧「设置」出现**描述输入框（多行 textarea）**；输入后自动保存。
- 描述显示在**节点标题下方**，多行自动换行（`word-break: break-all`），字号 11px 弱化显示，不遮挡节点正文。
- **技术要点（重要）**：描述不写入 SMM 富文本 `text`（simple-mind-map 的富文本序列化会把 HTML 转义/合并，污染标题），而是：
  1. 数据库 `mindmap_nodes.desc` 独立列存储（换行原样保留）；
  2. SMM 节点 `data.desc` 字段携带，标题保持纯文本；
  3. 渲染时在节点 group 下方自绘 `foreignObject` 描述块（自动换行、可随节点移动/缩放）；
  4. 编辑走 `setData({ desc })` + 重绘描述层，不触碰标题。
- 已兼容修复历史数据：早期版本误存进 `title` 的"标题+描述"HTML 会在加载时自动拆分回纯文本标题与描述。

### 7. 已知技术坑（供后续维护）

- **SMM 富文本序列化**：`SET_NODE_TEXT` 传入含 `<p>` 的 HTML 后，`getData()` 返回的 text 会被规范化（内层 `<p>` 被转义、多段 `<p>` 可能合并），因此"标题 + 描述"用富文本拼接的方案不可靠，必须用独立字段 + 独立渲染层。
- **折叠预览 / 描述层属于 SVG foreignObject**，不受 Vue scoped 样式作用，样式需写在全局（第二个非 scoped `<style>`）。
- 所有删除（导图/节点/文件等）遵循全局约定：软删 → 回收站 → 30 天后惰性自动清理，工作台显示清理提示。



---

## 第十二章 思维导图第 3 轮迭代（2026-09-08）

### 1. 界面布局调整

- **右侧「设置」面板已删除**，节点颜色 / 9 种形状 / 描述输入框合并到**左侧「布局」页签**的主题色块下方；单击画布节点后才显示（未选中时显示提示文字）。画布自动铺满原右侧区域。
- **布局切换改为按钮组**：左侧 5 个紧凑按钮（自由 / 向右 / 向左 / 组织 / 放射），两行排列，按钮间 2px 间距，文字为正常高度。
- **大纲高亮行**：选中行使用全局标签底色（--kh-brand #409eff）蓝底白字，与浏览页文件标签一致，替代原浅蓝半透明底。
- **导图库卡片置顶按钮**位于卡片**右上角**（top 8px / right 10px），不再遮挡左侧名称。

### 2. 右键菜单（导图库 + 大纲）

- 菜单使用 position: fixed + 鼠标视口坐标（clientX/Y），不再受滚动/容器定位影响——此前 absolute 定位在滚动后严重错位。
- 大纲行右键：添加子主题 / 添加同级 / 重命名 / 删除节点（删除进回收站）。

### 3. 折叠交互

- 分支节点折叠按钮**光标悬停节点时显示**（lwaysShowExpandBtn: false），不再常驻。
- **折叠预览已移除**：折叠节点下方不再显示子节点名预览条，画布更干净。

### 4. 手动保存（取消自动保存）

- 所有编辑操作（节点增删改、颜色/形状/描述、布局/主题、概要/边界/关系线）仅标记「未保存」状态，不再 600ms 自动落库。
- 工具栏新增**「保存」按钮**：有未保存修改时亮起（主题色），点击后全量保存（导图元信息 + 节点 + 关联线 + 边界/概要成员）。
- 状态提示：未修改 / 未保存 / 已保存 / 保存失败。
- 有未保存修改时**返回导图库**或**打开其它导图**，弹出确认框：「保存并返回 / 放弃修改」。

### 5. 折叠状态持久化（新增）

- mindmap_nodes 表新增 expand 列（1=展开，0=折叠），随保存全量写入；
- 重新打开导图时恢复各分支折叠状态，跨会话保持一致。

### 6. 放射（鱼骨）布局修复（依赖 simple-mind-map 源码补丁）

> ⚠️ 补丁位于 web/smm-patches/Fishbone.js（node_modules 被 git 忽略），重装依赖后需按
> web/smm-patches/README.md 恢复，并删除 web/node_modules/.vite 缓存后重启前端。

- **折叠跳位修复**：折叠中间分支后，右侧同级分支不再整体前移（如六顶思考帽折叠红/绿帽后黑帽位置保持不变）。原理：Fishbone.adjustLeftTopValue 新增模块级缓存 expandedWidthCache（uid → 最近一次展开渲染的子树水平宽度），折叠节点占位改用缓存宽度，兄弟节点 left 累加起点不再塌缩。
- **展开间距紧凑**：构造 MindMap 传 ishboneDeg: 58（默认 45°），斜线更陡、三级节点更贴近二级节点，横向占位明显减小（约压缩 1/4）。

## 第十三章 思维导图第 4 轮迭代（2026-09-08）

### 1. 节点描述渲染在节点内部（替代节点外浮层）

- 第 2 轮描述是节点矩形**下方**的独立浮层；本轮改为渲染在**节点矩形内部底部**。
- 实现基于 simple-mind-map 官方 `addCustomContentToNode` 钩子：
  - `create(node)`：读取 `node.nodeData.data.desc`，有描述时生成 `{ el, width, height }`（HTML 块，按行拆 div、自动换行），SMM 自动包成 foreignObject 追加进节点 group；无描述返回 null；
  - `handle({ content, element, node })`：渲染后把 foreignObject 平移到节点内底部（`node.height - 描述高 - 内边距`）；
  - **补丁 `nodeLayout.js` 的 `getNodeRect()`**：节点宽高计入追加内容尺寸（宽取 max(原宽, 描述宽+内边距)、高在原高上累加描述高）——否则描述会溢出节点矩形。补丁备份在 `web/smm-patches/nodeLayout.js`，与 Fishbone.js 一起按 README 恢复。
- `saveNodeDesc` 改为 `node.setData({ desc }) + node.reRender()`，描述编辑即时在节点内更新。
- 描述数据仍存独立字段 `mindmap_nodes.desc`，不写入 SMM 富文本标题（避免序列化污染）。

### 2. 放射（鱼骨）布局横向距离（继续压缩）

- `fishboneDeg`：58° → **72°**（分支斜线更陡，三级节点更贴近二级节点）；
- 放射布局下主题配置对二级节点 `marginX` 收紧（36 → 10）；
- 实测：二级节点 x=746、三级节点 x=896（SMM 逻辑坐标），**父子分支空隙约 70px**，较默认 45° 布局压缩约 2/3；
- 注：根节点→二级节点之间的主干长度由整棵子树高度决定（鱼骨布局固有），不随角度变化。

### 3. 布局按钮：加高 + 可视化图标

- 按钮 `padding: 7px`（高度约 28px，原 3px 太矮）；
- 每个布局前置内联 SVG 示意图标：自由=散点连线、向右=右向分支、向左=左向分支、组织=层级树、放射=鱼骨线（currentColor 跟随主题）。

### 4. 画布背景独立色板（幕布式）

- 左侧「主题」下方新增「画布背景」10 色色板：纯白 / 浅灰 / 米黄 / 浅蓝 / 浅绿 / 浅粉 / 深灰 / 深蓝灰 / 墨绿 / 深紫；
- 点击色板**覆盖主题背景**（themeConfig.background 与容器内联背景同步替换），并持久化到 `localStorage('kh-mm-canvasbg')`，刷新/重开保留；
- 设置了手动背景后，不再跟随系统深色自动切换（以手动选择为准）；
- 再次点击同色取消，恢复主题默认背景。

### 5. 导图库列表与卡片

- **卡片**：删除按钮 `✕` 移到**右下角**（hover 显示），与右上角置顶 📌 不再重叠；
- **列表**：文件名前新增思维导图类型图标（内联 SVG）；标签紧跟标题之后（不再被推到最右），节点数/更新时间右对齐——与浏览页"图标+名称+标签"风格统一。

### 6. 浏览页列表（资源管理器样式）

- 删除「类型」列：类型图标并入「名称」列，图标与文件名紧贴显示（类似 Windows 资源管理器列表模式）；
- 列头「标题」改为「名称」。

### 7. 帮助信息（面向最终用户）

- **节点描述**：选中节点后，在左侧「布局」页签的「描述」框中输入多行文字，即显示在节点矩形内部底部；用于补充说明、要点注释，不参与标题搜索。
- **画布背景**：左侧「画布背景」色板可独立调整画布底色，与主题互不冲突；系统深浅色切换时手动背景保持。
- **导图库**：卡片视图下删除按钮在右下角（悬停显示）；列表视图下文件前有导图图标，标签紧跟名称。
- **浏览页**：文件以「图标+名称」紧贴显示，不再单列类型。
